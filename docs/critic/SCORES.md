# Critic score log

Each entry is one loop: the commit that was scored, the per-dimension scores
from `RUBRIC.md`, the weighted overall, and the fixes proposed for the next
loop. Newest at the bottom.


## Loop 0 — 918f819 — 2026-09-08 — Overall 6.09

Tests: 52 pass / 0 fail. Playtest: 14 scenarios in ~10 s; the bot cleared level 1 on Beginner (x24, 25 kills, full life at tick 1500) but died before every mid/late/nightmare capture (Intermediate room 3: 14 kills; Expert room 6: 1 kill; Nightmare room 9: 29 kills), so those three PNGs show the debrief rather than a full wave; draw 0.4–0.8 ms at 34–52 enemies, 4–8 cached poses; simMs reads 0 because the bot path bypasses the loop's timer.

| # | Dimension | Score | Evidence |
|---|---|---|---|
| 1 | Core feel and controls | 6.5 | Fixed 20 ms step with render interpolation, edge-latched weapon keys, wheel/Q/E cycling, arrows as alternates, blur clears held keys (Input.ts:34-183). Deadzone follow camera clamped to the floor (Camera.ts:71-104, CAMERA.follow 0.18). Bite stun kills the controls for the slide (World.ts:876-880) as the original does. No gamepad; nothing measured by hand this loop. |
| 2 | Feedback and juice | 6.5 | Per-weapon muzzle flash, tracers, white hit flash at 30% (GameRenderer.ts:627-636), blood spray + drops + persistent decals, shake per weapon (pistol 1.2 → railgun 8, weapons.ts), blast flash, hurt vignette (GameRenderer.ts:974-984), "+N" popups, award/level banners (game-early.png "UZI+: Long Shot"). Gaps: the vignette decays in ~0.6 s (World.ts:2603) so there is no sustained low-health signal beyond a 30 px bar; multiplier steps down silently; level-up sound is a CLICK (World.ts:2676). |
| 3 | Readability and clarity | 5.5 | Score panel with drain bar and "NEXT x3 · PISTOL: FAST FIRE" goal pill (Hud.ts:212-226) is good; weapon strip shows ammo and empties in red; placement outline turns red when refused (GameRenderer.ts:648-663). But no off-screen threat indicator at all: game-start.png has enemies:2 and none on screen, and room 5 is 1792×672 against a 720×460 view. No "N left in wave" count; health bar is 30×5 px over the head. |
| 4 | Visual polish and consistency | 6.5 | Title key art and menus are strong and consistent (title.png, rooms.png, debrief skull). In play the HUD is a white-paper/red language (Hud.ts:26-29) against the brass/dark hint bar in the same frame (game-start.png), and the P quick-pause (brown band, Arial Black, quick-pause.png) is a different screen from the Esc pause menu (pause.png), which also blacks out the arena entirely. |
| 5 | Audio | 5.5 | 26 original samples with distance gain, stereo pan, per-sound voice caps and retrigger gap (AudioEngine.ts:112-153), ambient groans scaled by crowd, composed menu music with ducking. Coverage gaps: no award fanfare, level-up is CLICK, no player-hurt sound except the death scream (World.ts:2381), no multiplier-drop cue, no in-game music or ambience bed. Not audible in the headless run. |
| 6 | Onboarding and UX flow | 6.5 | Title → Play → room grid with a single unlocked room and clear "REACH LEVEL 4 TO UNLOCK" (rooms.png); 4.2 s control hint on entering a run (main.ts:271); complete How-to-play (howto.png); Esc is consistent everywhere (Menus.ts:438-458); pause focuses Resume so Enter works; multiplayer screen explains the wss:// limit. Menus are DOM buttons (Tab works) but there is no arrow-key navigation, and options/how-to scroll off the bottom at 720 px. |
| 7 | Difficulty curve and pacing | 6.0 | Wave tables are the original's (levels.ts: 5n+5, spawn 26-n, speed 1+n/10 cap 5), stragglers advance after 24 s (World.ts:144, 2648-2653), devils join early. The bot managed x24 by tick 1500 on Beginner, so the opening is not dead air. Expert start (level 20, 105 zombies at 3×) killed the bot after 1 kill (game-late.png) — no human read on fairness this loop; cheap deaths from own blasts are by design. |
| 8 | Progression and replayability | 5.0 | 18 arenas, per-room best and combined best, character skins cosmetic. Unlock check is `result.level >= 4` with no regard to the start level (SaveData.ts:253): Expert starts at 20, so dying at once unlocks the next room — game-late.png shows "LEVEL REACHED 20 · KILLS 1 · NEW ROOM UNLOCKED". One high-score per room across difficulties, so a x50 Nightmare start dwarfs any Beginner run. Debrief shows only score/level/kills; no peak multiplier, time, or accuracy. |
| 9 | Spirit of the original | 7.5 | Award ladder, banner text ("Infinate"), weapon numbers, 18 real layouts, vector puppets, original samples, pause band, skull debrief all recovered from the bytecode (upgrades.ts, weapons.ts, README "Where this port departs"). Mouse aim is the one departure a Boxhead player feels immediately; documented. |
| 10 | Performance and robustness | 6.5 | 52/52 tests incl. determinism, snapshot lockstep, netcode mirror. Draw 0.4–0.8 ms at 34–52 enemies (results.json), pose cache capped at 1200, decals at 900. ResizeObserver + zero-size guards (main.ts:355-373, Input.ts:141). Focus loss only clears keys (Input.ts:115-118): the sim keeps running while the tab is away, so a player alt-tabbing returns dead. Sim time not captured by the harness. |
| 11 | Multiplayer | 6.0 | Authoritative server, prediction + replay, one-minute seat hold with token reconnect (NetClient.ts:40-60), lobby with seats/ready/host config for co-op and deathmatch (Menus.ts:1011-1110), names over heads, frags HUD and "PLAYER n WINS" (Hud.ts:159-175). Netcode is test-covered but not exercised in this playtest; no live read on correction feel. |
| 12 | Accessibility and options | 4.5 | Volume, music, mute (M) persist; arrows duplicate WASD; Esc/P pause anywhere. No rebinding, no gamepad, no colour or text-size options (options.png). |

**Top fixes for next loop**
1. (dim 8, +~0.5) Close the unlock exploit and make bests honest: in `SaveData.recordRun` require `level >= startLevel(difficulty) + 3` (pass the start level from the session), record the difficulty next to each room best and show it on the room card, and add peak multiplier and time survived to the debrief and room record. ~1.5 h.
2. (dim 3, +~0.5) Off-screen threat arrows: in `Hud.draw`, for every alive enemy outside the camera rect draw a small triangle clamped to the screen edge (red for zombies, a larger orange one for devils), and add "N LEFT" to the score panel's level line from `zombieTotal + devilTotal - killed this level`. ~2 h.
3. (dim 10, +~0.3; also helps dim 1) Auto-pause on focus loss in single player: on `window` `blur` and `visibilitychange` (hidden), set `paused = true` in main.ts when a LocalSession is running, and show the quick-pause overlay so the player sees why. ~0.5 h. While in that overlay, restyle the P quick-pause to the brass/dark menu language so both pause screens match (dim 4, +~0.25).


## Loop 1 — 898d124 — 2026-09-08 — Overall 6.50

Tests: 52 pass / 0 fail. Playtest: 14 scenarios in ~10 s; the bot again cleared level 1 on Beginner (x26, 26 kills, full life at tick 1502) and reached level 10 at x27 on Intermediate (game-mid: 28 enemies on screen, full life), but died before the Expert (room 6: 2 kills, peak x30) and Nightmare (room 9: 16 kills, peak x59) captures, so those PNGs show the debrief. Draw 0.5–2.8 ms (up from 0.4–0.8 in Loop 0; 2.8 ms at game-early with only 2 enemies, 2.2 ms at 28), poses 3–15. simMs still reads 0 in five of six captures: `stepAverage` is an EMA fed only from `loop.step` (main.ts:464), which the bot path bypasses, so the commit's "real sim timings" claim holds for F3 in a live session but not for the harness. The control hint is still on screen at tick 2507 (game-mid.png) because its 4.2 s `setTimeout` is wall-clock and the bot runs 2500 ticks in under a second — a harness artifact, not a player-facing bug.

| # | Dimension | Score | Evidence |
|---|---|---|---|
| 1 | Core feel and controls | 6.5 → 6.5 | Nothing in Input.ts, Camera.ts or the player step changed. Auto-pause clears latches on blur (main.ts:389-398) so a held key cannot carry over a tab switch, which is a robustness win scored under dim 10. No gamepad; not measured by hand. |
| 2 | Feedback and juice | 6.5 → 7.0 | Two of the three gaps cited in Loop 0 are closed: the multiplier pops 35 % for 14 frames on each climb (Hud.ts POP_FRAMES, `ease`), the drain bar turns amber and flickers at 5-frame period in its last third so the drop is telegraphed (Hud.ts `urgent`/`flicker`; visible red bar under LEVEL 10 in game-mid.png), and a radial red heartbeat nags while health is under 30 % with period shortening as it worsens (Hud.drawLowHealth) — the sustained low-health signal that was missing. Low ammo reads amber in the strip. Level-up is now a two-thump drum, an award a two-note brass hit, an empty gun a dry click, and death plays the original's World.End sample (World.ts 2395, 2493-2496, 2683). Still no cue at the instant the multiplier actually steps down, and the heartbeat could not be seen in any capture (bot at 200 or 0 life), so this is code-read evidence. |
| 3 | Readability and clarity | 5.5 → 6.5 | Off-screen threat chevrons exist now (Hud.drawThreatMarkers): 24 angular buckets, nearest-per-bucket, size 7–13 px and alpha 0.35–0.9 by distance, orange for a devil. game-start.png shows two chevrons for the two unseen zombies; game-mid.png shows the wave arriving from the top edge as five chevrons plus three on the right. Rough edges: the chevrons are clamped to a 26 px margin and so sit on top of the score panel and goal pill in game-mid.png (975,30 and 1250,100), one bucket at 15° means a dense wave becomes a picket fence rather than a readable count, and there is still no "N left in wave" figure anywhere. Health bar is still 30×5 px over the head. |
| 4 | Visual polish and consistency | 6.5 → 7.5 | The whole in-game layer now speaks the menus' language: dark slabs with a brass hairline, Anton display, bone text, blood red (Hud.ts SLAB/BRASS/BONE constants; game-early.png score panel and weapon strip match the hint bar in the same frame). Banners are bone/brass with a black stroke and the level banner carries the menus' red rule. The Esc pause sits over the blurred, frozen arena instead of a black sheet (Menus.ts `.menu.overlay .bg` backdrop-filter; pause.png), and the P quick-pause is Cinzel + brass key legend under the same red band (main.ts drawQuickPause; quick-pause.png) so the two pause screens finally read as one. Remaining nits: chevrons collide with the score panel, and the room card still says "reach level 4 to unlock" (Menus.ts:629) while the debrief says "clear 3 more waves" — on Expert the card's number is simply wrong. |
| 5 | Audio | 5.5 → 6.0 | Coverage gaps closed: award fanfare (UI.Award, D→A square+triangle), new-wave drum (UI.Level), empty-weapon click (UI.Empty), and World.End on the run's end, all routed unpositioned at full level (AudioEngine.ts:125-140, synthesize()). Still missing: any player-hurt sound short of the death scream, a cue when the multiplier steps down, and an in-game music or ambience bed beyond crowd groans. The three new cues are oscillator synths beside 26 sampled sounds and were not heard in the headless run, so whether they sit in the mix is unverified. |
| 6 | Onboarding and UX flow | 6.5 → 6.5 | The debrief now tells the player exactly what the next unlock needs ("clear 3 more waves here to unlock the next room", Menus.ts:1152-1156; game-late.png), which is a real answer to "what do I do next". Against it, the room card's unlock text is static (Menus.ts:629) and contradicts the debrief on any preset but Beginner, there is still no arrow-key menu navigation, and options/how-to still run off the bottom at 720 px (howto.png). Net zero. |
| 7 | Difficulty curve and pacing | 6.0 → 6.0 | No change to levels.ts, spawn or straggler logic. Bot outcomes match Loop 0: Beginner comfortable (x26 at tick 1502), Intermediate survivable to level 10 at full life, Expert and Nightmare starts lethal within a minute. Still no human read on fairness. |
| 8 | Progression and replayability | 5.0 → 6.0 | The exploit is closed: unlock needs `level >= startLevel + UNLOCK_CLEARS` (SaveData.ts:322), so game-late.png (Expert, died at level 20) now shows "clear 3 more waves" where Loop 0 showed "new room unlocked". Each best remembers the difficulty it was set on and the card names it when it is not Beginner (SaveData.ts:312, Menus.ts:906); the debrief adds peak multiplier, time survived and difficulty (game-nightmare.png: x59, 0:50, NIGHTMARE), all of it also kept in the room record. Still one score slot per room across presets, so a x50 Nightmare start still overwrites and dwarfs a Beginner best — labelled now, but not separated; no run history, no per-difficulty tables. |
| 9 | Spirit of the original | 7.5 → 7.5 | The recovered ladders, weapons, layouts and samples are untouched. The HUD moved further from the original's paper look toward this port's own menu language, which is a documented design choice rather than a fidelity gain or loss; the red pause band was kept in both pause screens (main.ts drawQuickPause). |
| 10 | Performance and robustness | 6.5 → 7.0 | 52/52. The robustness flaw cited in Loop 0 is fixed: blur and visibilitychange pause a single-player run, correctly skipping NetSession, an open menu and a finished game (main.ts:389-398), so alt-tab no longer returns a corpse. Draw cost rose to 0.5–2.8 ms (results.json) — every HUD string is now drawn twice for the shadow (Hud.text) and markers are per-frame — still far inside a 16 ms budget but a trend to watch. Sim time is still not captured for the wave the bot fought (see the playtest note above). |
| 11 | Multiplayer | 6.0 → 6.0 | No netcode change. The frags panel and names picked up the new HUD language (Hud.drawScore deathmatch branch), and auto-pause is correctly disabled for NetSession. Not exercised live this loop. |
| 12 | Accessibility and options | 4.5 → 4.5 | options.png is unchanged: volume, music, mute, difficulty, speed, devils. No rebinding, gamepad, colour or text-size options. The heartbeat and chevrons are colour-only signals (red/orange) with no shape or text alternative, which does not help here. |

**Top fixes for next loop**
1. (dim 3, +~0.5) Finish the threat layer: add `N LEFT` to the score panel's level line from `levelInfo.zombieTotal + devilTotal - killedThisLevel` (or expose a `remainingInWave` getter on World), and keep chevrons off the HUD by clamping their inset rectangle below the score panel/goal pill (top-right 220×80 px) and above the weapon strip (bottom 60 px) in `Hud.drawThreatMarkers`. Merge adjacent buckets with a small count badge when more than three markers share an edge. ~1 h.
2. (dim 5, +~0.5) The three cues still silent: a short player-hurt grunt on any bite/fireball hit (pitch-shift the existing death scream to 1.3× and cap at one per 300 ms, or synthesize a 90 ms low square with noise like `thump`), a descending two-tone `UI.Drop` when `multiplier` steps down in World's drain path, and a quiet looped ambience bed (low filtered noise, gain scaled by `enemies.length`) under the groans so the arena is never dead silent. ~1 h.
3. (dim 6, +~0.5) Menu keyboard navigation and honest unlock copy: in `Menus`, handle ArrowUp/Down/W/S (and Left/Right in the room and character grids) to move focus between `.btn`/`.card` elements with Enter to activate, so the whole game is playable without touching the mouse between rounds; and make the room card read `clear ${UNLOCK_CLEARS} waves on ${difficultyName()} to unlock` instead of the static "reach level 4" (Menus.ts:629). ~1 h. After these, dim 12 is the lowest score on the sheet and the next candidate is a rebinding table in Options (~2 h, +~1.0 on weight 0.6).


## Loop 2 — 9045202 — 2026-09-08 — Overall 6.83

Tests: 52 pass / 0 fail. `npm run build`: clean (shared tsc + client tsc/vite, 201.6 kB bundle). Note: the working tree and the running dev server were one commit ahead (7323c64 "Rebindable keys"), so the screenshots are of 7323c64; the only difference is the rebinding table, which sits below the fold in options.png and is not scored here (it is Loop 3's dim 12 evidence). Playtest: 14 scenarios in ~12 s; the bot cleared level 1 on Beginner (x24, 25 kills, full life at tick 1501), reached level 10 at x34 with full life on Intermediate (game-mid: 22 enemies), and died before the Expert (room 6: 1 kill, peak x30) and Nightmare (room 9: 20 kills, peak x62) captures, so those PNGs show the debrief. Draw 0.6–2.2 ms (Loop 1: 0.5–2.8), poses 5–12. simMs still 0 in five of six captures (harness bypasses `loop.step`, as noted in Loop 1); the control hint is still on screen at tick 2502 for the same wall-clock reason. The reticle, hit stop, slow motion, rumble and every sound are invisible to the headless bot and are judged from the code.

| # | Dimension | Score | Evidence |
|---|---|---|---|
| 1 | Core feel and controls | 6.5 → 7.0 | A standard-mapping pad now plays the whole game: stick/d-pad move, right stick aims 180 px out from the player and falls back to facing the walk direction as the original did, RT/A fire, bumpers cycle, Start pauses; whichever device spoke last owns the aim and a 2 px mouse move takes it back (Input.ts `pollGamepad`, `aimWorld`, `updatePointer`). Both LocalSession and NetSession route aim through `aimWorld`. The mouse gets a drawn reticle whose gap springs out by `cooling² × (6 + shake×2)` per shot so the railgun kicks 22 px and the pistol 8 (Hud.drawReticle), with the browser cursor hidden only while the mouse aims and the player is alive (main.ts `#view.aiming`). Rough edges found in the code: a pad-only player cannot reach the pause menu at all (Start only toggles the quick pause, B is unhandled in play, and `Menus.pollPad` returns while `current === 'none'`), so Options and Quit need a keyboard mid-run; A pressed on RESUME fires a round on the first step back because `padFire` is level-triggered and `padHeld` is stale after a menu; the hit stop skips the step while `Loop.tick` keeps draining the accumulator, so the interpolated picture snaps back up to one step's motion rather than holding still; and the first run's hint always says WASD even with a pad, since `padSeen` is only set inside a run. Not measured by hand. |
| 2 | Feedback and juice | 7.0 → 7.5 | Kills now have weight: 1–4 held steps by kill count, spaced 12 ticks so an uzi never slideshows (main.ts HIT_STOP_FRAMES/SPACING), pad rumble scaled by kills and a hard 140 ms rumble on any hit (`input.rumble`), and the player's own fall runs at 0.4× for the 75-tick linger (`DEATH_SLOW_MOTION`), which at 50 ms/step is 3.75 s of slow motion before the debrief. The last two cues cited in Loop 1 exist: a grunt on every non-fatal hit (World.ts `UI.Hurt`) and a falling minor third when the multiplier steps down, one per second (World.ts `lastDropCueTick`, snapshot-safe). The reticle kick closes the loop between hand and gun. Nits: a one-step hit stop is 20 ms and, given the accumulator drain above, reads as a hitch rather than a freeze; 3.75 s of slow motion is on the long side for a game whose original cut straight to the skull. |
| 3 | Readability and clarity | 6.5 → 7.0 | "N LEFT" is on the score panel's level line (Hud.drawScore, `World.waveRemaining` = unspawned + standing): 9 LEFT in game-start.png, 1 LEFT in game-early.png, 21 LEFT in game-mid.png — the count that was missing for two loops. Chevrons are pushed out of the 240×84 score panel and the 600×66 weapon strip (Hud.drawThreatMarkers): game-mid.png has the top-right marker at (990,30) beside the panel instead of on it, and the bottom two at y≈640–648 sitting just above the strip. Still: one bucket per 15° means the wave from the top in game-mid.png is a picket of four chevrons rather than a count, the marker at (475,650) touches the pistol slot, and the health bar is unchanged at 30×5 px over the head. |
| 4 | Visual polish and consistency | 7.5 → 7.5 | Everything new speaks the established language: the reticle is bone ticks with a blood-red dot and a black shadow pass (Hud.drawReticle), the Feel section is the same brass labels and red sliders as Sound (options.png), room cards read "CLEAR 3 WAVES IN ROOM n TO UNLOCK" (rooms.png), the how-to gained a Gamepad row (howto.png), and the tab has a boxhead favicon (index.html). Nothing regressed. The Options page still runs off the bottom at 720 px (options.png cuts at "PROGRESS"), now with a longer page. Hold. |
| 5 | Audio | 6.0 → 7.0 | All three gaps from Loop 1 are closed and the arena is no longer silent between groans: GameMusic.ts is a 280-line tension-driven bed (D-minor drone whose filter opens with tension², a kick that quickens 72→132 bpm, bass past 0.25, hats past 0.45, snare past 0.7, a minor-second pad past 0.8), fed each step from creatures within 420 px, multiplier/60 and hurt fraction (main.ts `tensionOf`), rising fast and falling slowly, following the music slider, ducking under the Esc menu and fading on the run's end. UI.Hurt is a dropping triangle plus thump, UI.Drop a falling minor third, both unpositioned at full level like the other UI cues (AudioEngine.ts:125). Rough edges: the quick pause (P) and the auto-pause on blur do not duck the bed because the loop returns before `gameMusic.setScene` and only `onScreen` ducks; in a hidden tab the 80 ms scheduler is throttled to 1 s so the beat drops out while the drone holds; in co-op a partner's grunt arrives through NetSession.ts:407 as a `UI.` sound and plays at full level in the centre, indistinguishable from your own. All of it unheard in the headless run; this is a code-read score. |
| 6 | Onboarding and UX flow | 6.5 → 7.0 | The whole game is playable without a mouse: arrows/W-S walk focus through every visible button, select, input and link with Enter to activate (Menus.ts `moveFocus`, `focusables`), the d-pad/left stick do the same with A to choose, B/Start to back out and slow key repeat (Menus.pollPad), and buttons/cards already had `:focus` styling (Menus.ts:251, 315). The room card's unlock copy is finally true ("clear 3 waves in room 1 to unlock", rooms.png) and matches the debrief. The control hint adapts to a pad. Rough edges: focus order is linear, so Down in the 4-wide room grid moves one card to the right, not down; once a slider or select has focus the arrows are native and there is no way back out except Tab; the first run's hint ignores the pad; and options/how-to still scroll off at 720 px. |
| 7 | Difficulty curve and pacing | 6.0 → 6.0 | No change to levels.ts, spawn or straggler logic. Bot outcomes match both prior loops: Beginner comfortable, Intermediate survivable to level 10 at full life, Expert and Nightmare starts lethal within a minute (1 and 20 kills). Still no human read and no number for fairness. |
| 8 | Progression and replayability | 6.0 → 6.0 | No change to SaveData bests or the debrief (game-late.png and game-nightmare.png are identical in content to Loop 1). One score slot per room across presets; no run history. |
| 9 | Spirit of the original | 7.5 → 7.5 | Ladders, weapons, layouts and samples untouched; the pad faces the way it walks when the right stick is idle, as the original did (Input.ts:459-464). The bed, reticle, hit stop and slow motion are modern additions the original never had; GameMusic.ts documents the bed as deliberately not a tune so it does not fight the gunfire, and README's departures list covers the rest. Deliberate and documented, so no move. |
| 10 | Performance and robustness | 7.0 → 7.0 | 52/52, build clean, snapshot lockstep tests still pass with the new optional `lastDropCueTick` field (Snapshot.ts:192). Draw 0.6–2.2 ms across all captures, marginally better than Loop 1. Against it, four new rough edges from the code read: the pad cannot open the pause menu, A on RESUME fires, the hit stop's accumulator drain, and the un-ducked quick pause; `Menus` also polls the pad on a 50 ms `setInterval` for the life of the page. None of them loses a run. Sim time is still not captured by the harness. Hold. |
| 11 | Multiplayer | 6.0 → 6.0 | NetSession now aims through `aimWorld` so a pad works online, but `consumePause` is only called for local sessions so Start does nothing in a match (Esc still works). Hit stop and slow motion are correctly local-only. A partner's hurt grunt plays as your own (see dim 5). Not exercised live. |
| 12 | Accessibility and options | 4.5 → 5.5 | Gamepad in play and menus with a rumble toggle, screen shake 0–100 %, blast flashes off, HUD size 80–140 %, all persisted in SaveData and applied live through `onFeel`/`applyFeelSettings` (options.png Feel section). Menus need no mouse. Still no rebinding at this commit (it landed in 7323c64, next loop), no colour or high-contrast choice, and the chevrons/heartbeat remain colour-only. |

**Top fixes for next loop**
1. (dim 1 +~0.25, dim 10 +~0.25, dim 5 +~0.25) Close the four rough edges this loop's features shipped with, all in the client: (a) in `Input.pollGamepad` latch B (or Y) into a `menuPressed` flag that main.ts maps to `menus.show('pause')`, so a pad can reach Options and Quit; (b) add `Input.syncPad()` that reseeds `padHeld` from the live pad and holds `padFire` off until A is released, and call it from `onScreen` when the menus close, so RESUME never fires a round; (c) make the hit stop a real freeze by drawing with `alpha = 1` (reuse the `frozen` branch) and not draining `Loop.accumulator` while `hitStop > 0`; (d) call `gameMusic.setScene('ducked')` in the `if (paused) return` branch so P and the blur auto-pause duck the bed like the Esc menu does. ~1 h.
2. (dim 8, +~0.5) Give the debrief a story and the bests a fair table: store `bests[roomId][difficulty]` instead of one slot and show the current preset's best on the room card; add to the debrief the delta against that best ("+12,400 over your best"), accuracy (shots fired vs shots that hit, both countable in World's fire/hit paths), longest kill streak (World already tracks `streak`) and favourite weapon by kills; keep the last ten runs in SaveData and list them under Progress in Options. This is what a Vampire Survivors or Crimsonland reviewer would ask for first: a reason to press PLAY AGAIN that is not just the number. ~2 h.
3. (dim 7, +~0.5) Put a number on fairness and fix the first minute of the high presets. Add a `--preset expert|nightmare` mode to `tools/playtest.mjs` that reports ticks survived and kills so the curve is measured every loop, then, if the disassembly at the fidelity audit's location confirms the original pre-granted the award ladder below the starting multiplier, do the same in World's constructor (an Expert start at x3 with 105 zombies and a bare pistol is a cheap death); if it did not, keep the pistol start but hold the straggler advance and the multiplier drain for the first `waveGrace` of a preset start, documented as a departure. ~1.5 h. Loop 3 should also score 7323c64's rebinding table under dim 12 (expected +0.5 to +1.0 on weight 0.6).


## Loop 3 — c2d239a — 2026-09-08 — Overall 7.02

Tests: 52 pass / 0 fail (unchanged count: none of the six commits adds a test). `npm run build`: clean (shared tsc + client tsc/vite, 213.3 kB bundle, up from 201.6). HEAD, working tree and the dev server all at c2d239a this time. Playtest: 14 scenarios in ~10 s; the bot cleared level 1 on Beginner (x24, 25 kills, full life at tick 1501) but, despite the commit's claim that the new sixteen-heading retreat "reaches the late waves", it died before every high capture again: Intermediate room 3 dead at tick 2500 with 11 kills (Loop 2 had it alive at level 10, x34, full life), Expert room 6 with 2 kills, Nightmare room 9 with 12 kills; only the dedicated debrief scenario ran long (room 1 Nightmare, 2:00, 34 kills, x77). So game-mid/late/nightmare are debrief screens once more. Draw 0.4–3.2 ms (the 3.2 is the first capture at tick 151 while poses are still being cached; the rest 0.4–1.4), poses 5–9. simMs still 0 in the harness. The reticle, hit stop, slow motion, tips, the parked-run Continue button and every sound are invisible to the headless bot (fresh profile, no wall-clock) and are judged from the code.

| # | Dimension | Score | Evidence |
|---|---|---|---|
| 1 | Core feel and controls | 7.0 → 7.5 | The camera now leads toward the aim: 18 % of the pointer's world offset from the player, or 70 % of the cap along the pad's facing, capped at 64 world px before the deadzone/0.18 lerp (main.ts `aimLead`, `AIM_LEAD_MAX`); in every gameplay PNG the character sits at (480,330), 160 px off centre toward the bot's idle pointer and still inside the middle third. Three of Loop 2's four rough edges are closed: B or Y latches `menuPressed` and the loop opens the pause menu, online too (Input.ts `consumeMenu`, main.ts:696); `setEnabled(true)` now calls `syncPad`, which reseeds `padHeld` from the live pad and holds `padFire` off until every fire button is released, so the A that chose RESUME cannot fire (Input.ts `syncPad`, `padFireLatched`); and `frozen` includes `hitStop > 0`, so a held frame draws at alpha 1 where the world stands rather than part-way to a step it has not taken (main.ts:775). Diagonal key stacking is clamped to ±1 (Input.ts `buildCommand`). Nits: the look-ahead has no strength slider or off switch, and a mouse flick across the character swings the view by up to 128 world px in about 0.3 s, which some players will call swimmy; Y used to be next-weapon and now opens the menu (how-to updated, muscle memory not); `padSeen` is still only set inside a run, so the first run's hint ignores the pad. Not measured by hand. |
| 2 | Feedback and juice | 7.5 → 7.5 | The hit stop is now a true hold (see dim 1) instead of the hitch Loop 2 described, which removes that nit; nothing else in the feedback chain changed. 3.75 s of death slow motion is still long. Hold. |
| 3 | Readability and clarity | 7.0 → 7.0 | The aim lead shows more of what the player is shooting at, which is a readability gain in practice, but 64 world px is modest and the Loop 2 findings stand: one chevron bucket per 15° pickets a wave from one side, the marker at the bottom still grazes the pistol slot, the health bar is 30×5 px over the head. No screenshot shows the lead against a crowd, so no move. |
| 4 | Visual polish and consistency | 7.5 → 7.5 | Everything new is in the house style: the Controls grid is brass labels and slab key caps with a blinking red "press a key" state (Menus.ts `.keygrid`, `button.key.listening`), the history table is the same muted bone with brass headers (`.history`), ONE-TIME TIPS with its SHOW THEM AGAIN cap sits in the Feel column (options.png), and the debrief's second stats row uses the existing `.stats` rhythm (debrief.png: accuracy / longest streak / most kills with). The title's Continue block reuses `.stats`. Options is longer again and the fold at 720 px now cuts at ONE-TIME TIPS, so Controls, Progress and the history are all below it. Hold. |
| 5 | Audio | 7.0 → 7.0 | The quick pause and the blur auto-pause now duck the bed (main.ts:705 `gameMusic.setScene('ducked')` in the `paused` branch; line 758 restores 'play'). The other two Loop 2 rough edges remain: the hidden-tab scheduler throttle drops the beat, and a co-op partner's grunt still plays centred at full level. No new cues (a tip has no sound). Hold at 7.0; the duck is a half-nit, not a half-point. |
| 6 | Onboarding and UX flow | 7.0 → 7.5 | Nine one-time tips fire at the moment they matter, once per save: first visible crate, first devil, the multiplier's first serious drain (x4+, under 40 % of the window), each placeable as it is awarded, and life under 40 % (main.ts `TIPS`, `offerTips`), one at a time with an 8 s gap (`tipCooldown = 400`) and never over the control hint; the copy is correct against the sim (health does regenerate, tuning.ts `regenPerTick` = full in 30 s; barrels are slot 4; charge packs press-to-place, press-to-blow). Options can turn them off or reset them (Menus.ts `#tips`, `#resetTips`). The run hint now reads the bound movement keys (`controlsHint` via `keysFor`), and a parked run comes back through CONTINUE on the title with room, level and score, then "run restored" in the hint. Rough edges: tips are single-player only by design but that is not said anywhere; the 'hurt' tip can land in the same second as a bite that is about to kill you; the room grid's focus order is still linear; options/how-to still scroll off at 720 px. |
| 7 | Difficulty curve and pacing | 6.0 → 6.0 | levels.ts, spawn and straggler logic untouched; Loop 2's fix 3 (a `--preset` measurement and a preset-start grace) was not taken. The bot's outcomes are as lethal as ever on the high presets (Expert 2 kills, Nightmare 12 kills within a minute) and this run's Intermediate death at tick 2500 says the 16-heading retreat is not a reliable read of fairness either. Still no number. Hold. |
| 8 | Progression and replayability | 6.0 → 6.5 | Bests are now per preset (`RoomRecord.byDifficulty`, `SaveData.bestAt`, with a fallback so older saves keep their one record), the room card shows the best for the preset in force or says "no Expert run yet · best N on Beginner" (Menus.ts:986-991), and the debrief tells a story: "+12,400 over the old one" / "3,200 short of your best" / "first run here on this difficulty" (debrief.png), plus accuracy, longest streak and most-kills weapon carried through snapshots (World.ts `stats`, Snapshot.ts optional `stats`). Options keeps the last ten runs (room, preset, score, level, kills, time). Against it, the headline stat is wrong on screen: game-late.png reads **ACCURACY 196 %**. `shotsFired` counts one per trigger pull ("a shotgun blast counts once", types.ts) while `shotsHit` counts once per pellet Shot with an empty `hits` set (World.ts:2003), and the shotgun fires three rays (weapons.ts `shots: 3`), so a shotgun run can read up to 300 %; rockets and grenades only count a hit when the projectile itself touches a creature, so a splash-kill run reads low. Cluster shells inflate it the same way. The history has no date column although `at` is stored, and it sits below the fold. Half a point for the feature, not the full point, because the first thing a player reads on the new debrief can be nonsense. |
| 9 | Spirit of the original | 7.5 → 7.5 | Ladders, weapons, layouts and samples untouched; the wave tables are still the original's. But the camera lead, the tips and above all a run that parks itself and resumes from the title are departures the original never had (an arcade run was one sitting), and none of the three is in README's "departs on purpose" list (README.md:79-97, which the rebinding commit did not extend). Deliberate, yes; documented, no. Hold at 7.5 with that on record; a README paragraph is the cheapest fix on this sheet. |
| 10 | Performance and robustness | 7.0 → 7.0 | 52/52 and a clean build, but roughly 500 lines of new client and shared code (bindings, parking, tips, stats, per-preset bests, history) landed with zero new tests, and the one bug found this loop (196 % accuracy) is exactly the kind a five-line test would have caught. Robustness work is real: `continueRun` wraps the restore in try/catch and drops a snapshot that will not load, `SaveData.parkRun`/`savedRun` swallow storage errors, the parked run lives under its own key so the 8 kB snapshot never bloats the save, `stats` is optional in the snapshot so older lockstep peers still validate, and `recordRun` and `startRun` both clear the park so a dead run cannot be continued. Draw 0.4–1.4 ms in play. Parking fires on blur, Esc, pagehide and beforeunload but not on a timer, so a hard tab crash with no prior blur still loses the run the commit says it protects. Hold. |
| 11 | Multiplayer | 6.0 → 6.0 | The pad's B/Y reaches the pause menu in a match as well (the `consumeMenu` check runs before the `!networked` branch), which closes one Loop 2 gap; `consumePause` is still local-only so Start does nothing online, the partner's grunt is still centred, and a networked co-op debrief records its best under whatever preset the local save happens to hold (`local?.difficulty ?? save.difficulty`). Not exercised live for the third loop running. Hold. |
| 12 | Accessibility and options | 5.5 → 6.5 | The rebinding table is in: eight actions (move ×4, fire, next, prev, quick pause), click the cap and press the key, Escape cancels, a key serves one action and is taken from wherever it was, DEFAULT KEYS restores, bindings persist and survive the scores reset (Input.ts `setBindings`, `keyName`; SaveData `setKey`/`resetKeys`; Menus.ts `button.key` capture on the window in the capture phase so the menu's own arrow navigation cannot swallow it). One-time tips have a toggle. The pad now reaches every menu. Gaps: rebinding replaces both defaults with one key (bind W and you lose ArrowUp, since `keys[action] = [code]`), mouse buttons cannot be bound (no `mousedown` in the capture), the pad is not remappable, Digit keys still fire weapon slots on top of whatever they are bound to, and R, M, F3 and Escape are global keys that would collide silently if bound; no colour or high-contrast choice, and the chevrons and heartbeat remain colour-only. |

**Top fixes for next loop**
1. (dim 8 +~0.5, dim 10 +~0.25) Make the accuracy honest and put the new code under test. Give each trigger pull a `volleyId` on its Shots (or simply pass the fire event's id into `fireProjectiles`) and count `shotsHit` once per volley on the first pellet that touches a creature; for launched weapons also count a volley as a hit when its blast damages a creature (`applyAffect` knows `affect.weapon`), so a rocket run does not read 12 %. Clamp the debrief at 100 % as a belt-and-braces. Then add tests in `packages/shared/test/sim.test.ts`: a shotgun volley with three pellets in a zombie reads one hit of one shot; a rocket splash kill reads one of one; `longestStreak` and `killsByWeapon` survive `snapshot()`/`restore()`; and `SaveData.recordRun` on Nightmare leaves a Beginner best alone (a pure function, no DOM). Add a date column to the history. ~1 h.
2. (dim 7 +~0.5) The fairness fix Loop 2 asked for and this loop skipped, now with a better bot to measure it: add `--preset expert|nightmare --runs N` to `tools/playtest.mjs` reporting ticks survived, kills and level per run so the curve has a number every loop (this run's Intermediate death would have shown as variance, not a regression). Then check the disassembly at the fidelity audit's location for whether the original pre-granted the award ladder below the starting multiplier; if so, do the same in `World`'s constructor so an Expert start is not x3 with a bare pistol against 105 zombies; if not, hold the straggler advance and the multiplier drain for one `waveGrace` after a preset start and add it to README's departures list. The wave tables stay. ~1.5 h.
3. (dim 12 +~0.5, dim 9 +~0.25) Finish the rebinding and write down the departures. Let a binding keep an alternate (second cap per action, or shift-click to add), accept `mousedown` in the capture so fire can go on a mouse button (map to `Mouse1`/`Mouse2` codes in `keyName`), refuse or warn on the global keys (Escape, R, M, F3) and on a Digit that would double as a slot, and add a "chevron shape" or high-contrast toggle so off-screen threats are not colour-only. In the same sitting add three lines to README.md:79 for the camera lead, the tips and the parked run. ~1.5 h. Multiplayer (dim 11) stays a code-read score until the harness can drive two clients against `npm start`; a `--coop` scenario in `tools/playtest.mjs` that starts the server, joins two headless pages and screenshots the lobby and a shared wave is the prerequisite for any move there, and would pay for itself by also fixing the partner-grunt and Start-online gaps when they are finally seen (~2 h, +~0.5 on weight 0.7).

## Loop 4 — ef4a7bc — 2026-09-08 — Overall 7.25

Tests: 60 pass / 0 fail (8 new: shotgun volley, miss, rocket splash, tallies through a snapshot, per-difficulty bests, the unlock rule, practice runs, the shared-screen tether). `npm run build`: clean (221.1 kB bundle, up from 213.3). HEAD, working tree and the dev server all at ef4a7bc. Playtest: 14 scenarios in ~10 s; Beginner room 1 at tick 1501 alive with 26 kills, x25, full life; Intermediate room 3 dead at tick 2500 with 8 kills again; Expert room 6 dead with 1 kill; but Nightmare room 9 this time *alive* at tick 2502 with 342 kills, level 36, x248, full life (game-nightmare.png), the first time a high preset has shown a live wave. Draw 0.8–1.3 ms, poses 6–14, simMs 0–0.13. Fairness (`--fairness 3`, room 1, 9000-tick cap): Beginner median 180 s (2 of 3 reached the cap, levels 6–8, 152 kills), Intermediate 180 s (2 of 3 at the cap, one death at 50 s, levels 11–13, 254 kills), Expert 12 s (12/12/16, 13 kills, never past level 20), Nightmare 10 s (10/10/10, 34 kills, never past level 35). Shared-screen play has no harness scenario, so I drove it by hand through `window.__game` (`save.setSharedScreen(true); startRun(rooms[0].id,'swat'); debugBot(600)`), took a screenshot, and dispatched ArrowRight to the window for 30 steps: P2 moved 77 px, P1 stayed at x=305, so the second seat's keys reach the sim through Input, LocalSession and World and nothing leaks to the first seat. The tether itself is judged from the code and its test. The block shadow is 0.28 alpha over 10–22 px and the top-face tone 0.06; neither is legible at the harness's 1280×720 nor in the 800×450 pane, so both are credited from the code and only faintly from the browser.

| # | Dimension | Score | Evidence |
|---|---|---|---|
| 1 | Core feel and controls | 7.5 → 7.5 | Nothing changed for the single player's hand: the aim lead, hit stop and pad latching are as Loop 3 found them, and the Loop 3 nits stand (no lead slider, Y-as-menu, `padSeen` still only set inside a run, Input.ts:400). The second seat is done the way the original's keyboard players had it: arrows move, Enter/right Shift/Numpad 0 fire, comma and full stop cycle, and aim is the way you walk or last walked (`aimWorldB`, `facingBX/Y`), with the pad handed to seat B when there are two (`padSeat`). With two seats the camera holds the midpoint of the living with no lead (main.ts:765-773) and sees 1.3× the arena (`SHARED_SCREEN_VIEW`); tether 520 against a 598-tall view keeps both on screen at full stretch with ~39 px to spare. P2 has no reticle or facing cue beyond the sprite's gun. Fire on a mouse button works because `onPointerDown` adds `Mouse1..4` to `down` and `seatAHeld('fire')` reads it. Hold. |
| 2 | Feedback and juice | 7.5 → 7.5 | Untouched: no diff hunk in the feedback chain (hit stop, shove, blood, shake, popups, slow motion). The shared screen shows one score popup stream for two shooters; not a regression. Hold. |
| 3 | Readability and clarity | 7.0 → 7.5 | A devil's edge marker is now a diamond (Hud.ts:206-212), so the one enemy to deal with first is told apart by shape, not only orange; Loop 2's colour-only marker nit is closed for the enemy that mattered. Blocks now sit on a shadow that falls lower-right and their top face takes the light from upper-left like the character shading (GameRenderer.ts `shadeBlocks`, `drawBlock` tone), painted once into the floor layer, which gives cover a depth read. Against it: the 15° picket, the 30×5 health bar and the marker that grazes the pistol slot are all still there, and on a shared screen the marker exclusion zone is still "300 px either side of centre" (Hud.ts) while the two strips now sit at 27 % and 73 % of the width, so a low marker can land on a strip; P2 gets no markers at all (`drawThreatMarkers` runs for `player` only). Half a point for the diamond and the depth, no more. |
| 4 | Visual polish and consistency | 7.5 → 7.5 | Everything new is in the house style: P1/P2 in brass over each strip and over the second head (browser screenshot), the reserved-key refusal is a `.note bad` under the same hint, the history's new *when* column reads "today 14:02" (Menus.ts `whenLabel`). The shadows are the first depth cue the arena has had and they follow the existing light direction. But Options is longer again (hint paragraph, key note, an extra column) and the fold at 720 px still cuts at ONE-TIME TIPS (options.png), and the shading is faint enough that I could not see it in any capture. Hold. |
| 5 | Audio | 7.0 → 7.0 | No audio hunk in the diff. On a shared screen P2's shots and grunts play through the same `present.playSound` as P1's; whether they pan relative to P1 or the camera is unchanged code either way. The hidden-tab throttle and the online partner's centred grunt stand. Hold. |
| 6 | Onboarding and UX flow | 7.5 → 7.5 | The new mode is signposted where it is chosen: a tick box on the room screen, an inline "player 2 BOND · gamepad, or arrows + Enter" line, a TWO PLAYERS entry in How to Play (howto.png), the run hint reads "P1 WASD + mouse  P2 arrows + Enter" (`controlsHint`), and the tick box is a real `<input>` so `focusables()` reaches it (Menus.ts:529). A refused binding says why instead of silently keeping the old key. But nothing Loop 3 docked is fixed: options/how-to scroll off at 720 px, the room grid's focus order is linear, tips fire for P1 only and never say so, and the second seat has no character picker beyond "the other face" (`setSharedScreen` picks the first character that is not P1's). Hold. |
| 7 | Difficulty curve and pacing | 6.0 → 6.0 | levels.ts, spawns and stragglers untouched; neither the preset grace nor the pre-granted ladder Loop 2 and 3 asked for was taken. What changed is that the curve now has a number: Expert 12 s and Nightmare 10 s on room 1, three runs each, the Nightmare three identical to the second; the kiting bot dies at the same tick every time in the first room, which says the opening there is a wall, not variance. Yet the same bot in room 9 at Nightmare stands at level 36 with 342 kills and full life (game-nightmare.png), so the harsh spot is room 1's size at the high starts, not the wave tables. Beginner and Intermediate reach the 180 s cap in 2 of 3. A measurement is not a felt change. Hold, with the numbers on record for the next loop to move against. |
| 8 | Progression and replayability | 6.5 → 7.0 | The headline stat is honest: a trigger pull is one volley (`stats.volleys`, `Shot.volley`, `PendingAffect.volley`) and `countHit` counts it once on the first pellet or blast that touches a creature, with a 100 % clamp in `recordRun`; the debriefs read 29 %, 74 % and 94 % (game-mid/late/debrief.png) where Loop 3 read 196 %. The history has its date column. Per-preset bests, the unlock rule and practice runs are now under test (save.test.ts). Two players sharing one score and one multiplier is a new reason to come back. Remaining: the history is still below the fold, there is no cross-room summary beyond BEST on the title, and `hitVolleys` is not in the snapshot, so a volley in flight when a run is parked can count twice after CONTINUE (a one-shot rounding error, not a 196). |
| 9 | Spirit of the original | 7.5 → 8.0 | The name is 2Play and this loop finally delivers it: two players on one screen, the second on the arrow keys aiming the way they walk as the original's keyboard players did, one shared score and multiplier, a fallen player returning beside the other (`respawn` with `respawnNear`, `Spawn_ValidPosition_Close`). And the README departures list Loop 3 asked for is written: camera lead, hit stop, slow-motion death, parked run, tips, bed, markers, statistics, tether (README.md:98-107). Half a point rather than one because the shared screen is co-op only: LocalSession never passes `mode`, so the original's on-one-screen deathmatch is still absent though `World` already supports it. |
| 10 | Performance and robustness | 7.0 → 7.5 | 60/60 with the eight tests Loop 3 asked for by name, a clean build, draw 0.8–1.3 ms at a full wave (342 kills, 56 enemies, 14 poses in game-nightmare.png at 1.1 ms). Older snapshots load (`volley?` optional in ShotSnapshot, `volleys ?? 0` on restore), a parked shared run restores its second seat (`secondCharacterId` through `parkRun`/`continueRun`), `hitVolleys` is capped at 32 so it cannot grow. Not there: no test drives `Input` for the seat routing (I did it by hand above), the 1.3× view's draw cost was not measured (69 % more floor per frame), and parking still fires on blur/pagehide but not on a timer. Half a point for the tests. |
| 11 | Multiplayer | 6.0 → 6.5 | Local co-op works end to end and I saw it: two seats, P1/P2 labels over the heads and the strips, the arrows driving only seat B, the pad handed to seat B (`padSeat`), the tether holding at ≤201 px in its test and always allowing the walk back. Gaps that keep it a half point: a keyboard P2 cannot pause (`SEAT_B.pause` is empty; only pad Start reaches `pausePressed`), P2 has no threat markers, no shared-screen deathmatch, and the strip exclusion zone in the marker code is wrong for two strips. Online is exactly as Loop 3 left it (Start does nothing in a match since `consumePause` is local-only, the partner's grunt is centred, a networked debrief records under the local save's preset) and still has not been exercised live. |
| 12 | Accessibility and options | 6.5 → 7.0 | Loop 3's three rebinding gaps are closed: shift-click adds an alternate (up to two, `setKey(..., add)` with `.slice(-2)`), a mouse button binds like a key (`mouseCode`, the `mousedown` capture in Menus, `keyName` reads "Right click"), and Escape/R/M/F3/Tab and the ten digits are refused with a note (`RESERVED_KEYS`). The devil marker is shape-coded. Still: the pad is not remappable and neither are P2's keys; the quick-pause overlay hard-codes "P RESUME" (main.ts:845) so a rebound pause key lies on screen; Mouse 4/5 are bindable but nothing suppresses Chrome's back/forward on their `mouseup` (unverified, but no handler exists); no high-contrast or colour choice, and the heartbeat is still colour-only. |

**Top fixes for next loop**
1. (dim 7 +~0.5) The fairness number points at the fix: room 1 kills the bot at 10 s on Nightmare three times out of three while room 9 lets it reach level 36. Give a preset start its grace: hold the straggler advance and the multiplier drain for one `waveGrace` after a start above level 1, and pre-grant the award ladder below the starting multiplier if the disassembly at the fidelity audit's location shows the original did (otherwise say so in README's departures). Add `--room N` to `--fairness` so the benchmark can be run on room 1 and room 9 and the two numbers compared each loop; the target is Expert and Nightmare medians that vary between runs instead of reading 10/10/10. ~1.5 h.
2. (dim 11 +~0.5, dim 3 +~0.25) Finish the shared screen. Give seat B a pause key (Backspace or Pause, in `SEAT_B.pause`) and a line in the hint; draw threat markers for each living local seat and move the strip exclusion zones to 27 %/73 % when `localSeats >= 2`; let the room screen choose the second character; and pass `mode: 'deathmatch'` from a second tick box so 2Play's own head-to-head exists on one screen (World and MODES already carry it, so this is UI and a `mode` field on `LocalOptions`). Add a `coop` scenario to `tools/playtest.mjs` that ticks the box, drives seat B with dispatched ArrowRight for 300 ticks and screenshots the result, so the next loop sees it without hand-driving. ~2 h.
3. (dim 12 +~0.5, dim 4 +~0.25, dim 6 +~0.25) Make Options fit and make its labels true. Split Options into tabs or collapsible sections (Game / Sound / Feel / Controls / Progress) so Controls and the history are reachable at 720 px without scrolling; make the quick-pause overlay and How to Play read `keyName` from the bindings instead of "P" and "Space"; add a `mouseup` handler that `preventDefault`s Mouse 4/5 when bound; and a high-contrast toggle that gives the heartbeat a shape as well as a colour. ~1.5 h.

## Loop 5 — dfde275 — 2026-09-08 — Overall 7.41

Tests: 61 pass / 0 fail (1 new: a progress code carries bests and unlocks and merges upward). `npm run build`: clean (229.2 kB bundle, up from 221.1). HEAD, working tree and the dev server all at dfde275. Playtest: 16 scenarios (two new, shared-coop and shared-deathmatch) in ~11 s; Beginner room 1 at tick 1501 alive with 26 kills, x25, full life; Intermediate room 3 alive at tick 2501 with 29 kills at level 10 (dead last loop); Expert room 6 dead with 1 kill; Nightmare room 9 alive at tick 2502 with 349 kills, level 36, x250, full life. Draw 0.5–2.4 ms (the 2.4 is the shared-screen 1.3× view at 14 poses, so its cost is now measured), simMs 0–0.31. Fairness room 1 (`--fairness 3`): Beginner 174 s (154/174/180, one at the cap), Intermediate 104 s (20/104/150, none at the cap, was 180), Expert 12 s (12/12/14), Nightmare 12 s (10/12/14). Fairness room 9 (`--fairness 2 --room 8`, the first time this number exists): Beginner 180/180, Intermediate 180/180, Expert 180/180 with 384 kills at level 23, Nightmare 150/142 with 503 kills at level 37. Nothing in the diff touches `packages/shared`, so the room-1 spread is the bot's variance (the browser's frame timing, since the bot reads the wall clock through `debugBot`), not a curve change. Co-op (`--coop`): a real server on 127.0.0.1:9644, two headless Chromes, Ann (swat, HOST) and Ben (bond) in the lobby with two OPEN SEATs and the match block (Room 1, Co-op, Beginner, Normal, devils) in coop-lobby.png; the match started from the host and both seats driven for 4.2 s; each client reports rtt 1–2 ms, server tick 211, unacked 2, replayed 1, corrections 2 (last 2.7–2.8 px). By hand in the browser pane at 1280×720: the Controls and Progress tabs both fit the fold (alternates read "W / ↑", the Progress tab holds the code field and the reset), and a shared deathmatch ended through `endMatch(0)` shows the versus debrief: "Player 1 wins", PLAYER 1 20, PLAYER 2 0, SURVIVED 0:02, REMATCH / CHOOSE ANOTHER ROOM / MAIN MENU on the skull watermark.

| # | Dimension | Score | Evidence |
|---|---|---|---|
| 1 | Core feel and controls | 7.5 → 7.5 | Nothing changed for the hand: no hunk in the aim lead, hit stop, pad latching or camera. Seat B gained a pause key (`SEAT_B.pause = ['Backspace']`, routed to `pausePressed` in Input.ts) and the thumb buttons left the bindable set (`mouseCode` returns null for 3 and 4), both of which belong to dimensions 11 and 12. The Loop 3 nits stand: no lead slider, Y-as-menu, `padSeen` only inside a run. Hold. |
| 2 | Feedback and juice | 7.5 → 7.5 | No hunk in the feedback chain. One correctness fix: online, a partner's hurt grunt no longer plays centred at full level as though you were hit (`presenter.playSound` checks `event.ownerId` against the local seats before `UI.Hurt` plays), so a phantom hurt cue is gone; a small thing, and P2's own feedback on a shared screen is as Loop 4 left it. Hold. |
| 3 | Readability and clarity | 7.5 → 7.5 | Gains: threat markers are drawn for every living local seat (Hud.ts:95-98, visible for both heads in shared-coop.png), the marker exclusion zone knows about two strips (`strips = [0.27w, 0.73w]`), the quick-pause overlay reads the bound key (`keyName(input.keysFor('pause')[0])`), and a ten-slot strip shrinks to fit a narrow window with a smaller face under 50 px. Loss: the fit budget for a side strip is `canvas.width / 2 - 16·s` but the strip is centred at 0.27·width, so at 720 px tall (HUD scale 720/620 = 1.16) a full arsenal runs 35→656 px and P2's 623→1244: the strips overlap by ~33 px and P1's RAIL slot sits under P2's PSTL in shared-deathmatch.png, where the whole arsenal is owned from tick one. The exclusion half-width (170·s = 197 px) is also smaller than the strip's real half-width (311 px), so a low marker can still land on the outer slots. Online, the partner is off screen in both coop-host.png and coop-guest.png (room 1 is 768 tall against a 720 view) and nothing points to them; only enemies get markers. A gain and a new defect cancel. Hold. |
| 4 | Visual polish and consistency | 7.5 → 7.5 | Options is five tabs in the brass-and-slab language (options.png; `.tab.on` carries the red underline and brass glow) and every tab fits at 720 px, which closes the fold nit Loops 3 and 4 docked. The versus debrief, the P1/P2 red-and-brass score panel in deathmatch and the "change" cap beside the second face are all in house style. Against it: the overlapping strips in shared deathmatch are the first visibly broken HUD since Loop 0, at every window height since the overlap scales with the HUD, and the versus debrief says "Player 1" where the room screen just showed the chosen faces. Hold. |
| 5 | Audio | 7.0 → 7.0 | One hunk: the partner-grunt fix above, closing the nit Loop 3 and 4 carried. Positional mix, music and the hidden-tab throttle are unchanged; a shared screen's second seat still plays through the same listener as the first. A closed nit is not a felt change on its own. Hold. |
| 6 | Onboarding and UX flow | 7.5 → 8.0 | Three docked items closed at once: Options fits the fold in every tab (Controls and Progress checked by hand), How to Play reads the real bindings (`keyLabel` over `save.keys`, howto.png shows the defaults), and the second seat has a face of its own (a "change" cap that steps past P1's, Menus.ts `#secondChar`). Deathmatch is chosen where the mode is chosen, with its rule in the label ("first to 20"), and the room screen's inline line now says Backspace pauses. The How to Play TWO PLAYERS entry explains both modes in five lines. Still: How to Play itself scrolls at 720 px (howto.png cuts at SURVIVING), the room grid's focus order is linear, tips fire for P1 only, and "change" cycles four faces rather than showing them. |
| 7 | Difficulty curve and pacing | 6.0 → 6.0 | No sim hunk. What the loop adds is the second number: room 9 lets the same bot survive Expert to the 180 s cap twice (384 kills, level 23) and last 142–150 s on Nightmare (503 kills), while room 1 kills it at 12 s and 10–14 s. The tables and the preset start levels are the original's and stay, so the curve is as it was; the number now says the felt harshness is room 1's 800×768 against a level-20 or level-35 opening, not the ladder. The multiplier-drain hold was declined as off-spirit; routes that do not touch the tables are in the fixes. Hold. |
| 8 | Progression and replayability | 7.0 → 7.5 | Progress travels: the Progress tab shows scores, unlocks and history as a `BH1.` code, copy puts it on the clipboard, paste-and-import merges (bests keep the higher per room and per difficulty, unlocks the farther, histories combine to ten), and the merge is under test (save.test.ts). The Pages site and the downloaded file can now share one record. Shared-screen deathmatch is a second reason to come back with a friend, with a rematch one click away. The Progress tab is reachable without scrolling. Still: no cross-room summary beyond combined best, the code is ~200 characters even with no history (not "short"), and there is no URL form. |
| 9 | Spirit of the original | 8.0 → 8.5 | The last missing 2Play mode is in: two players on one screen, no zombies, the whole arsenal owned but unloaded until a crate, first to twenty, and the original's "PLAYER n WINS" (`endMatch` pushes it as a level message, `renderVersusDebrief` repeats it). Player 2 on arrows, Enter, comma and full stop with Backspace to pause is how the original's keyboard pairs sat. Nothing recorded from a deathmatch, as the original recorded nothing. The departures list in README was not extended for the progress code or the tabs, but neither changes play. |
| 10 | Performance and robustness | 7.5 → 7.5 | 61/61 and a clean build; draw 0.5–2.4 ms, the shared 1.3× view measured at last (2.4 ms at 14 poses, against 1.3 for a single seat at 12); the online path is exercised by machine for the first time (a real server, two Chromes, corrections of 2.7 px). Against it: the two-strip overlap shipped in a new mode with no test that would catch it, and `importCode` checks only the prefix, the version and `typeof rooms`, so a record missing `score` reaches `Math.max(mine.score, record.score)` and persists NaN as a best (a truncated or hand-edited code can poison the save). `hostScreen`/`guestScreen` in coop.json report "lobby" at join time and nothing after, so the harness does not yet assert both reached the wave. Hold. |
| 11 | Multiplayer | 6.5 → 7.0 | Online is finally seen, not read: lobby with names, faces, HOST and NOT READY, two open seats and the match block; ready, start, both seats walking and firing on one server; rtt 1–2 ms, 2 unacked, 2 corrections at ≤2.8 px on each side. The pause key opens the pause menu on a server (`networked && input.consumePause()`), the partner's grunt is theirs, and the shared screen is complete: deathmatch with a winner's debrief and rematch, a second face, Backspace for the keyboard seat, markers for both, a hint line that names both seats. Not the full point: the deathmatch HUD overlaps at every window height for a full arsenal (P1 loses its last slot), the versus debrief and the kill target ("first to 20") never reach the in-run panel, the partner off screen online has no marker, reconnect is still not driven by the harness, and a networked co-op debrief still records under the local save's preset. |
| 12 | Accessibility and options | 7.0 → 7.5 | Every Options tab fits without scrolling; the quick-pause overlay and How to Play tell the truth about rebound keys (the Loop 4 "P RESUME lies" nit); the thumb buttons that Chrome keeps for back and forward are no longer offered; a keyboard second seat can pause; progress can be carried across browsers. Still: the pad is not remappable and neither are P2's keys, no high-contrast or colour choice, the heartbeat and the chevrons are colour-only, and the tab last opened is remembered for the visit only (`optionsTab` is on the Menus instance, not the save). |

**Top fixes for next loop**
1. (dim 11 +~0.5, dim 3 +~0.25, dim 4 +~0.25) Fix the two-strip fit and finish the deathmatch HUD. In `Hud.drawWeapons` give a side strip a budget of `2 * min(centre, width - centre) - 32·s` (0.46·width, since it is centred at 0.27 or 0.73), and derive the marker exclusion from the strip's real half-width instead of the hard-coded 170·s (return the extent from `drawWeapons`, or compute it the same way). Add a test in `packages/client/test` that lays out two ten-slot strips at 1280×720 and 1920×1080 and asserts they do not intersect; the `shared-deathmatch` scenario can assert the same through `window.__game`. Then put the kill target on the versus panel ("P1 0 · P2 0 · FIRST TO 20"), use the chosen faces' names in the versus debrief, and draw an off-screen partner as a bone-coloured chevron so online co-op can find each other. ~1 h.
2. (dim 7 +~0.5, dim 8 +~0.25) Fill the gap between Intermediate and Expert without touching the tables or the drain: a *custom* start level on the Game tab (1–40, its own bests key like the presets, or marked a practice run if that is off-spirit), so a player who found Intermediate easy has a step before level 20 in an 800×768 room. Beside it, let the room card say what the benchmark knows: keep `fairness.json` per room in `packages/client/src/data` and show "Expert here: brutal / fair" from the median, or add one line to the debrief ("most runs here on this preset last about 12 s"), so a 10-second Nightmare death on room 1 reads as the room's opening rather than the player's failing. Record both in README's departures. ~1.5 h.
3. (dim 10 +~0.25, dim 8 +~0.25, dim 12 +~0.25) Harden and shorten the progress code, and remember the tab. Validate every record on import (finite, non-negative `score`, `level`, `kills`, `plays`; skip or clamp anything else) so a truncated code cannot write NaN into a best; drop `tipsSeen` from the payload and accept a `#progress=BH1...` fragment on load so the transfer from Pages to the downloaded file is one link; put `optionsTab` in the save. In the same sitting, let the pad's face buttons and P2's keys be rebound in the Controls tab (a second column, same `keygrid`), which is the last rebinding gap Loop 3 named. ~1.5 h.

## Loop 6 — fd11e42 — 2026-09-08 — Overall 7.69

Tests: 72 pass / 0 fail (11 new: eight `Hud.layoutStrip` layout cases at 1280×720, 1920×1080, 800×450 and 2560×1440, a poisoned progress code, a practice start marking the run, and the custom start's multiplier curve). `npm run build`: clean (236.4 kB bundle, up from 229.2). Scored at fd11e42; while this pass ran, the working tree carried pad-rebinding work (Input.ts, Menus.ts, SaveData.ts, playtest.mjs) that the dev server served, showing as a Gamepad block on the Controls tab, and it landed as b8762c4 (then 2e5d08e) before this entry was written; nothing below credits it, it is Loop 7's to score. Playtest: 16 scenarios in ~11 s; Beginner room 1 alive at tick 1501 with 26 kills, x25, full life; Intermediate room 3 dead at tick 2500 (level 10, 10 kills, 0:50; alive last loop, so the bot's variance); Expert room 6 dead with 1 kill; Nightmare room 9 alive at tick 2502 with 323 kills, level 36, x226, full life. Draw 0.5–1.7 ms, simMs 0–0.04; shared co-op 1.3 ms at 19 poses. Fairness room 1 (`--fairness 3`): Beginner 180 s (all three at the cap, was 174), Intermediate 162 s (158/162/180, was 104), Expert 12 s (10/12/46), Nightmare 10 s (10/10/12). Fairness room 9 (`--fairness 2 --room 8`): Beginner 180/180, Intermediate 180/180, Expert 180/180 with 395 kills at level 23, Nightmare 102/32 with 449 kills (was 150/142). The only `packages/shared` hunk is a `killTarget` getter on World, so every one of those moves is the bot, not the curve. Co-op (`--coop`): server on 127.0.0.1:9509, Ann (swat, HOST) and Ben (bond) in the lobby with two open seats; both seats driven; rtt 1 ms, server tick 213, unacked 4, replayed 1, corrections 2 at 2.7–2.8 px on each side; in coop-host.png the partner is off screen below and a bone chevron sits at the bottom left, in coop-guest.png the partner is off screen above and the bone chevron sits at the top beside a red threat marker. By hand in the browser pane at 1280×720: the Feel tab holds CAMERA LEADS THE AIM and HIGH CONTRAST with its explanatory hint; the Options tab last opened is in `boxhead.save.v1` (`optionsTab: "feel"` after a visit); loading `/#progress=BH1...` with a foreign record merged it (rooms unlocked 2 / 18, combined best 12,345), landed on the Progress tab and cleared the hash. shared-deathmatch.png: two ten-slot strips, P1's ending at x≈632 and P2's starting at x≈648 at 1280×720, no overlap; the panel reads P1 0 · P2 0 with FIRST TO 20 beneath.

| # | Dimension | Score | Evidence |
|---|---|---|---|
| 1 | Core feel and controls | 7.5 → 7.5 | Two small gains for the hand: the camera lead can be switched off (`save.cameraLead` gates `aimLead` in main.ts), and the second seat, which aims the way it walks, gets a bone tick past its gun (`Hud.drawFacing`, visible as a short dash left of P2's pistol in shared-coop.png), so its shots are no longer a guess. A pad that speaks on a menu now counts for the hints (`onPadSeen`). Nothing else in the chain moved: aim lead, hit stop, latching and the camera follow are as Loop 3 left them, and the lead is a switch rather than the slider Loop 3 asked for. The tick is 9·s px long and easy to miss at 1.3× zoom. Hold. |
| 2 | Feedback and juice | 7.5 → 7.5 | No hunk in the feedback chain beyond the high-contrast frame on the heartbeat, which is an accessibility choice (dim 12). P2's own feedback on a shared screen is still P1's leftovers: `drawLowHealth` runs for `localPlayerIndex` only, so the keyboard seat has no heartbeat and no hurt vignette of its own. Hold. |
| 3 | Readability and clarity | 7.5 → 8.0 | The Loop 5 defect is gone and the missing marker arrived. Strips: `Hud.layoutStrip` gives a side strip `2·min(centre, |w/2 − centre|) − 16·s` of room, and shared-deathmatch.png shows the full arsenal twice with a 16 px gap between P1's RAIL and P2's PSTL. Markers: the exclusion now reads the strips' real extent (`stripExtents` pushed from `drawWeapons`, consumed a frame later as `lastStripExtents`), and game-nightmare.png has two markers resting exactly on the strip's top edge at x≈445–475 instead of under it. Off screen partner: a bone chevron for any connected, living teammate outside the view (`partners` in `drawThreatMarkers`), seen in both coop PNGs, so an online pair can find each other in a room taller than the window. The deathmatch panel says FIRST TO 20 so the rule is on screen for the whole match. Still: the partner chevron and the devil marker are shapes of the same size as a zombie marker and differ by colour alone unless high contrast is on; the facing tick is small. |
| 4 | Visual polish and consistency | 7.5 → 8.0 | The one visibly broken HUD is fixed at every size the test covers, and the versus debrief now names the faces (`P1 Swat` / `P2 Bond` through `CHARACTERS.find` in `recordRun`) where Loop 5 docked "Player 1". The Game tab's PRACTICE START row and its hint, the Feel tab's two new rows and the Progress tab's second button all sit in the slab-and-brass language (options.png; the hint hangs under the slider on the label column). The in-run panel's FIRST TO 20 uses the same 8·s brass caption the winner line uses. Nits: the practice hint's negative margin is an inline style rather than a class, and the slider's "off" position is level 1 on a 1–60 range, which reads oddly beside the preset dropdown's "level 1". |
| 5 | Audio | 7.0 → 7.0 | No hunk. The shared screen's second seat still plays through the first seat's listener; no per-seat pan, no music change. Hold. |
| 6 | Onboarding and UX flow | 8.0 → 8.0 | The room grid is walked by position now: `moveFocusToward` scores candidates by along + 2.5·across and Down goes to the card below for arrows and the d-pad alike (the pad's left stick maps to buttons 14/15), closing the Loop 5 "focus order is linear" nit. A pad seen on a menu makes the first hint name the pad. A run parks itself every 500 ticks as well as on blur, so a closed tab or a crash leaves CONTINUE on the title (seen in the pane after the playtest). A progress link lands on the Progress tab with "progress merged from the link". Against a move: How to Play still scrolls at 720 px (howto.png cuts at SURVIVING), tips fire for P1 only, "change" still cycles four faces blind, and the practice slider gives no idea which level is "the wave after Intermediate" (its ticks are unlabelled; the presets sit at 1, 10, 20, 35). Hold. |
| 7 | Difficulty curve and pacing | 6.0 → 6.5 | The tables and preset starts stay the original's, and the numbers say the same thing as Loop 5 (room 1 kills the bot at 10–12 s on Expert and Nightmare, room 9 lets it reach the cap on Expert). What changed for the player is the step that was missing: a practice start on any level from 2 to 60 with the multiplier the presets' own curve banks there (`multiplierForStart`: 1→x1, 10→x10, 15→x20, 20→x30, 35→x50, extrapolated past 35, under test), so the player who cleared Intermediate can open level 15 in room 1 before facing level 20. The run is marked practice on the Game tab, at the start and in the debrief (`practiceReason` "custom start at level N", "not recorded"), and it parks and continues as practice (`startLevel` on the parked run). Not more: the room card and the debrief still say nothing about what a preset does in that room, so a 10-second Nightmare death in room 1 still reads as the player's failing, and the practice start is on the Options screen rather than beside the difficulty on the room screen. |
| 8 | Progression and replayability | 7.5 → 8.0 | Progress travels by link: `exportLink` makes `origin + pathname + #progress=` and the load path merges it, clears the hash and opens the Progress tab (checked by hand: a foreign record became best 12,345 and unlock 2); "import what is pasted" accepts the link as well as the bare code. `tipsSeen` left the payload. A practice run is a new reason to open a room again, on its own terms and recorded as nothing. Still: the code is 216 characters with no history (not short), `location.origin` is the string "null" for a file:// page in Firefox, so a link copied from the downloaded boxhead.html there begins "null/", and there is no cross-room summary beyond combined best. |
| 9 | Spirit of the original | 8.5 → 8.5 | The practice start is a departure and README's list says so, with the rule that it records nothing. The multiplier at a custom level is drawn from the original's own four points, not invented. The versus debrief names faces where the original said PLAYER n; the panel's FIRST TO 20 is the original's rule. Nothing in the tables or the drain moved. Hold. |
| 10 | Performance and robustness | 7.5 → 8.0 | 72/72 and a clean build. Both Loop 5 defects closed with tests: the strip overlap (`hud.test.ts`, four sizes, two-strip and single-strip cases) and the NaN poison (`importCode` runs every record and history entry through `count`, which admits only finite non-negative numbers; `save.test.ts` feeds a record without a score, a string score, a negative score, a string unlock count and a history entry without a time, and every one is dropped). Timed parking (`world.tick % 500`) means a crash costs at most ten seconds; the snapshot write measured nothing in `results.json` (simMs 0.02 at 52 enemies) though the harness samples one frame, not the parking frame. Draw 0.5–1.7 ms. Not the full point: `hostScreen`/`guestScreen` still read "lobby" and nothing asserts both clients reached the wave; the dev server served a tree ahead of fd11e42 for the whole pass (the pad rebinding, committed only afterwards as b8762c4), so what was scored and what was served were not the same build; `exportLink` on file:// is untested. |
| 11 | Multiplayer | 7.0 → 7.5 | The four things Loop 5 named are done: the deathmatch HUD no longer overlaps at any tested size, the kill target is on the panel (FIRST TO 20 in shared-deathmatch.png), the winner's debrief names the faces, and an off-screen partner gets a bone chevron online (both coop PNGs). Prediction is as before: rtt 1 ms, 2 corrections at ≤2.8 px each side. Still: reconnect is not driven by the harness, a networked co-op debrief records under the local save's preset, and the practice start does not reach the lobby's match block (a host cannot open an online run on level 15). |
| 12 | Accessibility and options | 7.5 → 8.0 | High contrast is a real mode: threat markers take a 2.5·s white outline and the heartbeat gains a white frame that thickens with the beat (`drawLowHealth`), so neither rests on red alone; the camera lead can be turned off for players the lean unsettles; the Options tab last opened is in the save (`optionsTab`, checked in localStorage); a pad on a menu is recognised. Still, at fd11e42: the pad's face buttons and P2's keys are not remappable (the pad half landed after this pass as b8762c4, for Loop 7), text size is only the HUD slider, and the partner chevron differs from a zombie marker by colour alone even in high contrast. |

**Top fixes for next loop**
1. (dim 2 +~0.5, dim 5 +~0.5, dim 3 +~0.25) Give the second seat its own answers on a shared screen. In `Hud.drawLowHealth` run the heartbeat for every local living seat, drawing seat B's as a ring or a bar under its head (or the left/right half of the frame, with the high-contrast frame split the same way) so two low players do not merge into one vignette; play the hurt grunt and the low-health tick for seat B at its own pan; and set the shared-screen listener at the midpoint of the two seats (or pan each seat's sounds by its screen x) instead of P1's position. While there, give the partner chevron a distinct shape (a ring, not a wedge) so it never reads as a threat in either contrast mode. ~1 h.
2. (dim 7 +~0.5, dim 6 +~0.25) Put the benchmark and the practice start where the death is. Ship a small per-room, per-preset table (`packages/client/src/data/benchmarks.ts`, the medians from `fairness.json` for rooms 1 and 9 now, more as they are run) and print one line on the room card and the debrief: "on Expert most runs here last about 12 s". On the debrief add a button "practise level N here" (N = the level the player died on) that sets `save.startLevel` and restarts, and label the practice slider's ticks with the preset names at 1, 10, 20 and 35 so the gap between Intermediate and Expert is visible on the control itself. Move the slider (or a copy) to the room screen beside DIFFICULTY. ~1 h.
3. (dim 12 +~0.5, dim 10 +~0.25, dim 8 nit) Finish the rebinding and close the small holes. The pad half (`setPadBindings`, the Controls tab's Gamepad block) landed as b8762c4 after this pass; give it a test that a rebound fire button fires and that the defaults survive an empty save, then add P2's keys as a second column of the same `keygrid`; make `exportLink` use `location.href.split('#')[0]` so a file:// page in Firefox does not produce "null/…"; and have `--coop` in `tools/playtest.mjs` record `hostScreen`/`guestScreen` after the match starts and assert both read the wave (b8762c4's plug-pull covers reconnect, not this). ~1.5 h.

## Loop 7 — e25b06e — 2026-09-08 — Overall 7.84

Tests: 72 pass / 0 fail (no new tests; the ~150 lines of pad and seat-B binding code, `setKeyB`, `setPadButton`, `resetKeys` clearing both, and the file:// `exportLink` are all untested). `npm run build`: clean (240.68 kB bundle, up from 236.4). `git status` clean, so the dev server served e25b06e for the whole pass. Playtest: 16 scenarios in ~12 s; Beginner room 1 alive at tick 1501 with 25 kills, x23, full life; Intermediate room 3 dead at tick 2500 (level 10, 10 kills, 0:50); Expert room 6 dead with 0 kills at 1:00 (level 20, x30 peak); Nightmare room 9 dead at tick 2500 with 112 kills, level 35, x108 peak (alive last loop; the bot's variance). Draw 0.5–2.5 ms, simMs 0–0.02; shared co-op 1.8 ms at 21 poses. Fairness room 1 (`--fairness 3`): Beginner 180 s (all three at the cap, =), Intermediate 180 s (146/180/180, was 162), Expert 12 s (10/12/14, =), Nightmare 10 s (10/10/12, =). Fairness room 9 (`--fairness 2 --room 8`): Beginner 180/180, Intermediate 180/180, Expert 180/180 with 385 kills at level 23, Nightmare 94/10 with 409 kills (was 102/32). Nothing under `packages/shared` changed, so every move is the bot. Co-op (`--coop`): server on 127.0.0.1:9892, Ann (swat, HOST) and Ben (bond) in the lobby with two open seats; `bothInWave: true` (both clients' `screen` is "none" at tick 214); `reconnect`: the guest's socket closed at tick 214, it came back to seat 1 (`seatBefore` 1, `seatAfter` 1) in state `joined` and had simulated to tick 265 when polled; rtt 1–2 ms, server tick 264, host unacked 3 / replayed 2 / corrections 3 (last 2.4 px), guest unacked 1 / replayed 0 / corrections 2 (last 2.2 px). In coop-host.png Ben is on screen by the crate with his name over him and in coop-guest.png Ann is on screen at the top right, so this run's PNGs do not exercise the new off-screen ring; it is judged from `Hud.drawThreatMarkers`. By hand in the browser pane: the Controls tab at 1280×720 holds three blocks (P1, "Player 2 on the keyboard", "Gamepad: click a button and press the new one") and DEFAULT KEYS AND BUTTONS, the last two below the fold; P2's Quick pause rebound to Delete through the page's own click and a keydown with `code: "Delete"` (`keysB.pause: ["Delete"]` in `boxhead.save.v1`, the button reading "Delete"), and the reset put Backspace back and dropped `keysB` and `pad` from the save. The pane's own key injection did not rebind (it carries no `code`), which is the harness, not the game. options.png: the practice slider's preset labels overprint — BEGINNER, INTERMEDIATE and EXPERT collide into one smear at x≈305–435 — and the label row starts ~27 px left of the track, so no tick sits under its label. debrief.png / game-mid.png / game-late.png / game-nightmare.png: PRACTISE LEVEL 10 / 20 / 35 HERE sits between PLAY AGAIN and CHOOSE ANOTHER ROOM.

| # | Dimension | Score | Evidence |
|---|---|---|---|
| 1 | Core feel and controls | 7.5 → 7.5 | Nothing in the movement, aim, fire or camera chain moved; the pad's `fire`/`next`/`prev`/`menu`/`pause` now read from `this.pad` (`Input.ts`) but resolve to the same buttons by default, and `seatAAction` now also yields any key seat B owns when two share the keyboard, which is correct and unfelt. The Loop 3 lead slider is still a switch; the facing tick is still 9·s px. Hold. |
| 2 | Feedback and juice | 7.5 → 8.0 | The second seat on a shared screen is answered at last: `Hud.draw` picks the living local seat with the lowest `life / maxLife` (`localSeats` loop) and runs the heartbeat for it, and the hurt grunt goes through a `StereoPanner` at `(x − listener.x) / halfWidth` clamped ±0.7 (`AudioEngine.play`, `UI.Hurt` branch) with `event.x` the hurt player's own position (`presenter.playSound`), so a bitten P2 on the right hears it on the right. Not the full fix Loop 6 asked for: the vignette is still one whole-screen wash with nothing that says whose it is (a full-life P1 sees the screen pulse red for P2's wound), two low seats merge, and there is no low-health tick cue at all (`UI.*` is Award, Click, Drop, Empty, Hover, Hurt, Level). Not seen in a screenshot: no shared-screen scenario reaches low life. |
| 3 | Readability and clarity | 8.0 → 8.0 | The partner marker is a shape now, not a colour: a 7·s ring with a 4·s dark halo and a 2·s bone stroke, and a 1.8·s dot at the partner's facing (`drawThreatMarkers`, `partners`), so it cannot be read as a chevron in either contrast mode. Unexercised by this run's coop PNGs (both partners on screen). Against it, a new ambiguity on a shared screen: the heartbeat now belongs to "whichever seat is worst off" without saying which, and the devil marker still differs from a zombie marker by colour alone. One nit closed, one opened. Hold. |
| 4 | Visual polish and consistency | 8.0 → 7.5 | One visibly broken control shipped. The `.ticks` row under the practice slider positions each preset name at `(startLevel − 1) / 59` of a 260 px box at 9 px with 0.12 em tracking: BEGINNER, INTERMEDIATE and EXPERT (at 0, 40 and 84 px) overprint into an unreadable smear in options.png, and the row's `margin-left: 146px` starts ~27 px before the track (x≈305 vs x≈332), so NIGHTMARE also sits left of its tick. On the Controls tab P2's FIRE wraps to two lines ("ENTER / R SHIFT / NUM 0") while every other key button is one line, and P1's rows show the arrows as secondaries (W / ↑) directly above a P2 block that claims the arrows. The debrief's PRACTISE button and the two new Controls blocks are otherwise in the slab-and-brass language, and the ticks are inline styles again. |
| 5 | Audio | 7.0 → 7.0 | One cue moved: the hurt grunt is panned to the hurt seat (`UI.Hurt` in `AudioEngine.play`), the only UI sound that was position-blind while the world's shots and pickups already panned by camera x. Mix, coverage and music are as before; the shared screen's listener is still the camera, which is right for two seats. A nit closed, not a change in coverage. Hold. |
| 6 | Onboarding and UX flow | 8.0 → 8.0 | The practice start is where the death is: "Practise level N here" on every non-practice debrief from level 2 (`result.level >= 2 && !result.practice`), which sets `save.startLevel` and restarts the same room with the same face (game-mid.png, level 10 in room 3). Two rough edges arrive with it. The button arms a persistent state: after one practice, PLAY AGAIN, every room and the next session all open as practice (the room screen's "practice run — custom start at level N" badge says so, `practiceReason`) until the player finds the slider on the Options Game tab, and the practice debrief hides the button so there is no way back from where they came. The slider's preset labels, meant to show the gap between Intermediate and Expert, are unreadable (dim 4). How to Play still scrolls at 720; the Controls tab now scrolls too. A gain and a trap; hold. |
| 7 | Difficulty curve and pacing | 6.5 → 7.0 | The numbers say what they said (room 1 kills the bot at 10–12 s on Expert and Nightmare; room 9 lets it cap on Expert; Nightmare 94/10) and the tables are the original's. What changed for the player is the step Loop 6 named: the wave that ended the run is one click away with the multiplier a preset banks there (`multiplierForStart` via `setStartLevel(result.level)`), so the level-20 wall in room 6 becomes "practise level 20 here" rather than a return to level 10 on Intermediate. The ticks exist on the control (`datalist#presetTicks` gives the thumb detents at 1/10/20/35) even though their labels do not read. Still nothing tells the player what a preset does in a room before they die in it. |
| 8 | Progression and replayability | 8.0 → 8.0 | `exportLink` builds from `location.href.split('#')[0]`, so the downloaded boxhead.html in Firefox no longer yields "null/#progress=…" (untested, read from the diff). The practise button is a new reason to reopen a room, still recorded as nothing. The code is still 216 characters with no history; no cross-room summary. Hold. |
| 9 | Spirit of the original | 8.5 → 8.5 | Nothing in the tables, the drain or the arsenal moved; the practise button is the documented departure reached from a new door. README's Controls paragraph still says "every key but the weapon numbers can be rebound" and describes the pad by its default buttons, saying nothing of the pad or P2 being rebindable; a stale line, not a spirit matter. Hold. |
| 10 | Performance and robustness | 8.0 → 8.5 | 72/72, clean build, clean tree, draw 0.5–2.5 ms. Two of Loop 6's three holes closed with measurements rather than hope: `--coop` records each client's `screen` after the drive and asserts `bothInWave` (true), and it closes the guest's socket mid-wave and reports the seat it came back to (1 → 1), its state (`joined`) and that it went on simulating (214 → 265). `exportLink` on file:// is fixed. Withheld: not one test for the new binding code, so a rebound pad fire button firing, `setKeyB` stealing a key from the other action, defaults surviving an empty save and `resetKeys` clearing `pad`/`keysB` are all unproven by `npm test`; `hostScreen`/`guestScreen` still read "lobby" beside the new `screen` field; the pad-rebind listener polls at 40 ms with no timeout, so a click with no pad attached listens until Escape. |
| 11 | Multiplayer | 7.5 → 8.0 | Reconnect is measured now, not asserted: the guest lost its socket at tick 214 and was back in seat 1, joined and simulating within the poll (`reconnect` in coop.json). Both clients reaching the wave is asserted. An off-screen partner is a ring with a dot online (unexercised by this run's PNGs). Prediction as before (rtt 1–2 ms, corrections ≤ 2.4 px). Still: a networked co-op debrief records under the local save's preset; the practice start does not reach the lobby's match block; the host's reconnect is not driven. |
| 12 | Accessibility and options | 8.0 → 8.5 | Both of Loop 6's named gaps closed. The pad's fire, next, previous, menu and quick-pause buttons rebind from the Controls tab by pressing the new button (`setPadButton`, one button per action, buttons ≥ 12 refused so the d-pad stays), and P2's eight keys rebind in a second `keygrid` (`setKeyB`, verified by hand: `keysB.pause` became Delete and the reset restored Backspace); `main.ts` installs both at boot and after every change. Still: the pad block is below the fold at 720; text size is only the HUD slider; `keyName` falls back to the raw code ("Delete", "Backspace", fine; "IntlBackslash" would not be). |

**Top fixes for next loop**
1. (dim 4 +~0.5, dim 6 +~0.25, dim 7 nit) Make the preset ticks read and give practice an off switch. Drop the absolute-positioned `.ticks` row in `Menus.ts`; wrap the range in a block the width of the track (the input's own width, not a hard 260 px at a 146 px margin) and put the four names on two staggered rows (Beginner and Expert above, Intermediate and Nightmare below) with no letter-spacing, each centred on `(startLevel − 1) / 59`; better still, fold the name into the readout ("level 15 — between Intermediate and Expert"). Then add a one-click way out of practice: a "practice off" chip beside the room screen's practice badge and on the practice debrief's verdict line, both calling `setStartLevel(0)`, so "Practise level N here" is a detour and not a mode the player is stuck in. ~45 min.
2. (dim 7 +~0.5, dim 8 +~0.25, dim 6 nit) Tell the player what a preset does in a room from their own record, not a bot's. `SaveData` already keeps per-room, per-preset records and a history with times: print on the room card and the debrief "your runs here on Expert: best level 23, median 0:42 over 5" and, when the history is empty for that preset, the preset's own line ("Expert opens at level 20 with x30"). On a death inside 30 s at a preset the player has never cleared 3 waves on, make PRACTISE LEVEL N HERE the primary button and PLAY AGAIN secondary. Wire `startLevel` into the lobby's match block so a host can open an online practice run too. ~1 h.
3. (dim 10 +~0.25, dim 12 +~0.25, dim 2 +~0.25, dim 3 nit) Prove the bindings and finish seat B's heartbeat. Tests in `packages/client/test`: `setPadBindings({ fire: [2] })` makes X fire and RT not; `setKeyB('pause', 'Delete', …)` stores Delete and removes it from any other seat-B action; an empty save yields `DEFAULT_PAD` and `DEFAULT_SEAT_B`; `resetKeys` clears `pad` and `keysB`. Give the pad-rebind listener a 10 s timeout and a "no gamepad found" note. Then split the heartbeat by seat in `drawLowHealth`: centre the radial gradient on the hurt seat's screen x (left or right half when it is P2) and split the high-contrast frame the same way, so P1 knows whose beat it is and two low seats read as two. ~1 h.

## Loop 8 — 08dbfc1 — 2026-09-08 — Overall 8.05

Tests: 74 pass / 0 fail (2 new: seat-B and pad rebinding stealing a key and resetting together; a room's record line from the player's own runs). `npm run build`: clean (244.79 kB bundle, up from 240.68). `git status` clean, so the dev server served 08dbfc1 for the whole pass. Playtest: 16 scenarios in ~12 s; Beginner room 1 alive at tick 1501 with 25 kills, x24, full life; Intermediate room 3 dead at tick 2500 (level 10, 10 kills, 0:50, as last loop); Expert room 6 dead at tick 3000 with 5 kills at 1:00 (x30 peak; 0 kills last loop); Nightmare room 9 dead at tick 2500 with 12 kills at 0:50 (112 kills last loop; the bot's variance, nothing under `packages/shared` changed). Draw 0.5–2.7 ms (the 2.7 is the first capture at tick 152), simMs 0–0.25; shared co-op 1.1 ms at 20 poses. Fairness room 1 (`--fairness 3`): Beginner 180 s (all three at the cap, =), Intermediate 154 s (150/154/180, was 146/180/180), Expert 10 s (10/10/14, was 10/12/14), Nightmare 10 s (10/10/14, =). Fairness room 9 (`--fairness 2 --room 8`): Beginner 180/180, Intermediate 180/180, Expert 180/180 with 401 kills at level 23, Nightmare 98/10 with 397 kills (was 94/10). Co-op (`--coop`): server on 127.0.0.1:9885, Ann (swat, HOST) and Ben (bond) in the lobby with two open seats; `joinedAs: { host: "lobby", guest: "lobby" }` (the renamed field), `bothInWave: true` at tick 214; `reconnect`: seat 1 → 1, state `joined`, tick 214 → 266; rtt 1–2 ms, server tick 264, host unacked 3 / replayed 0 / corrections 2 (last 2.8 px), guest unacked 2 / replayed 0 / corrections 3 (last 2.7 px). In coop-host.png Ben is on screen by the crate and in coop-guest.png Ann is on screen at the top right, so the off-screen ring is unexercised for the second loop running. By hand in the browser pane at 1280×720: the Controls tab holds the three columns (Player 1, Player 2, Gamepad) and DEFAULT KEYS AND BUTTONS above the fold, P2's FIRE reads "ENTER / R SHIFT / NUM 0" on one line; the practice slider at 15 reads "level 15, between Intermediate and Expert" beside the NO HIGH SCORES badge with only the datalist detents under the track; the room screen shows the red practice badge with a PRACTICE OFF cap beside it; a pushed level banner in a level-15 run reads "-+-+-+- LEVEL 15 -+-+-+-" over the red rule with "80 ZOMBIES · 4 DEVILS" 4·s px beneath it (banner-zoom.png), no overlap. `world.messages` is empty at tick 23 of a run, so the first wave of a run, the one a preset start opens on, gets no banner and no count. A scratch script against `SaveData` (`rec.mts`): three Expert runs in r1 give `{ runs: 3, bestLevel: 21, medianSeconds: 20 }`; ten Beginner runs in r2 afterwards give `{ runs: 0, bestLevel: 21, medianSeconds: 0 }` for r1 Expert, because `recordLine` reads the ten-entry global history and returns non-null on the best alone; a practice run enters neither. howto.png at 1280×720 still cuts at the TWO PLAYERS entry and BARRICADES, so the two-column How to Play scrolls at that size despite the commit's claim. The hover tick and click knock are judged from the code (headless, no ear).

| # | Dimension | Score | Evidence |
|---|---|---|---|
| 1 | Core feel and controls | 7.5 → 7.5 | No hunk in movement, aim, fire, weapon switching or the camera; `Input.ts` changed only `keyName`. The Loop 3 lead slider is still a switch, the facing tick still 9·s px. Hold. |
| 2 | Feedback and juice | 8.0 → 8.0 | One of the three gaps Loop 7 listed is closed: on a shared screen the heartbeat's radial gradient centres on the hurt seat's screen x, clamped to the middle half (`drawLowHealth(ctx, worst, seatX)`, `cx = clamp(seatX, 0.25w, 0.75w)`), and the high-contrast frame's top and bottom bars cover that seat's half only, so a full-life P1 can tell the pulse is P2's. Still one vignette for the worst seat only (two low seats merge), no low-health tick cue, and no scenario reaches low life, so it is read from the diff, not seen. Hold. |
| 3 | Readability and clarity | 8.0 → 8.5 | Two ambiguities named in earlier loops are answered. The level banner says what the wave holds: `levelDef(world.level).zombieTotal` and, with devils on, `devilTotal`, in 10·s bold bone under the red rule (banner-zoom.png: "80 ZOMBIES · 4 DEVILS" under LEVEL 15), and it is suppressed in deathmatch where it would lie. The heartbeat says whose it is on a shared screen (dim 2). Withheld from more: the count arrives only at a level-up, never on the wave a preset start opens on (`messages` empty at tick 23), which is the wave that kills the bot in 10 s; the devil marker still differs from a zombie marker by colour alone; the wave line sits 4·s px under the rule, tight at 720. |
| 4 | Visual polish and consistency | 7.5 → 8.0 | The one visibly broken control is gone: no `.ticks` row, the datalist detents alone under the track and the preset named in the readout (options.png, and "level 15, between Intermediate and Expert" in the pane); `button.key { white-space: nowrap }` puts P2's FIRE on one line; the Controls tab is three columns at ≥ 1180 px and fits the fold at 1280×720 with the reset button visible; How to Play is two columns of the same `dl.keys`. Back to Loop 6's 8.0, not past it: the three Controls columns do not align (the pad's hint wraps to three lines so its rows start ~40 px lower than the others'), PRACTICE OFF is a key cap (`button.key`) doing a chip's job beside a `.badge`, the debrief's new record line duplicates the stats row beneath it on a first run ("best level 10, usually 0:50" over "level reached 10 · survived 0:50"), "Num Add" and "Num Subtract" where a keyboard says + and −, and the hint margins are still inline styles. |
| 5 | Audio | 7.0 → 7.5 | The menus had no sound at all; now every button, unlocked card, tab, key cap and back link gives a 25 ms 1.9 kHz triangle tick as the pointer or the focus lands (`pointerover` with a `relatedTarget` guard so a child does not re-fire, `focusin` for the keyboard and the pad) and a square 520 Hz knock with a short thump when chosen (`UI.Hover`, `UI.Click` in `AudioEngine.synthesize`; `onUiSound` in main.ts, played centred). That is coverage the rubric asks for and the pad's d-pad walk gets it through `focusin`. Judged from code. Nits: `focusin` also fires on programmatic focus, so every screen that focuses its first control on open (`target.focus()`, `#resume`) ticks once unasked; a `select` knocks on opening the dropdown as well as on the choice; the hidden-tab throttle stands. |
| 6 | Onboarding and UX flow | 8.0 → 8.5 | The trap Loop 7 named is closed: PRACTICE OFF sits beside the practice badge on the room screen (seen in the pane) and beside the "practice run — not recorded" verdict on the practice debrief, both `setStartLevel(0)` and re-render, so "Practise level N here" is a detour again. The slider says where a level sits among the presets (`startLevelLabel`: "where Expert opens", "between Intermediate and Expert", "before", "past"). A room never played says what the preset does ("not played · Beginner opens at level 1 with x1", rooms.png). A sub-30 s death on a run that cleared under three waves leads with PRACTISE LEVEL N HERE as the primary button (`quickDeath`). Controls fits the fold. Against: How to Play still scrolls at 1280×720 (howto.png cuts at TWO PLAYERS and BARRICADES), the card lost the "best N on Beginner" line it had since Loop 3 (a save with best 231,500 on another preset reads "no Beginner run yet · Beginner opens at level 1 with x1"), and `quickDeath` reads this run's clears rather than whether the player ever cleared the preset here, so a veteran's quick death is told to practise too; tips are still P1-only. |
| 7 | Difficulty curve and pacing | 7.0 → 7.5 | The tables, preset starts and drain are the original's and the numbers say what they said: room 1 kills the bot at 10 s on Expert and Nightmare three times out of three, room 9 lets it cap on Expert and last 98/10 s on Nightmare. What changed for the player is the framing Loop 7's fix 2 asked for: the room card names where the preset opens before they die in it, the debrief says what their own runs here have done ("your runs here on Expert: 1, best level 20, usually 1:00", game-late.png) and, after a quick death, offers the wave that killed them first. Each level-up now says how many are coming. Not more, because the count never reaches the opening wave of a preset start (see dim 3) and the record line is only right for the player's last ten runs (see dim 8). |
| 8 | Progression and replayability | 8.0 → 8.0 | The player's own record per room and preset exists and is under test (`recordLine`: runs, best level, median seconds; `save.test.ts`), on the card ("… · 3 runs, usually 0:20") and the debrief. But it is derived from the ten-entry global `history`, so "runs" means "runs among your last ten anywhere", and once a room's entries age out the debrief prints "your runs here on Expert: 0, best level 21, usually 0:00" (`rec.mts`: three Expert runs, then ten elsewhere; the card guards on `runs > 0`, the debrief does not). On a first run the line repeats the row beneath it, and the card no longer shows the best on another preset. A feature and a defect: hold. The code is still 216 characters; no cross-room summary. |
| 9 | Spirit of the original | 8.5 → 8.5 | Nothing in the tables, the drain or the arsenal moved. README's controls paragraph now says the second seat and the pad rebind (the stale line Loop 7 noted). The wave count under the banner is a small addition the original did not have; it changes no rule and is off in deathmatch. Hold. |
| 10 | Performance and robustness | 8.5 → 8.5 | 74/74 with the binding tests Loop 7 asked for by name (`setKeyB` steals Enter from fire, shift-click adds beside the first, `setPadButton` takes A from fire, `resetKeys` clears `keysB`, `pad` and `keys`), a clean build and tree, draw 0.5–2.7 ms. The pad-rebind poll gives up after ten seconds and says whether it saw a pad (`started`, "no gamepad found: press a button on it first"). `--coop` names its join-time field honestly (`joinedAs`). Withheld: the record line's aging defect shipped with a test that never ages the history, and PRACTICE OFF, `quickDeath` and `startLevelLabel` are untested; the `focusin` tick fires on programmatic focus. Hold. |
| 11 | Multiplayer | 8.0 → 8.0 | Reconnect measured again (seat 1 → 1, `joined`, 214 → 266), both clients in the wave, rtt 1–2 ms, corrections ≤ 2.8 px. On a shared screen the heartbeat belongs to a seat. Still: a networked co-op debrief records under the local save's preset, the practice start does not reach the lobby's match block (Loop 7's fix 2 asked for it; not taken), the host's reconnect is not driven, and the partner ring is unexercised by the harness for the second loop. Hold. |
| 12 | Accessibility and options | 8.5 → 8.5 | Two of Loop 7's three named gaps closed: the pad block is above the fold at 1280×720 in the three-column Controls tab, and `keyName` no longer shows a raw code (Numpad → "Num …", Intl → the suffix, CapsLock → "Caps", ContextMenu → "Menu", anything else split into words). The rebind poll cannot hang on a missing pad, and the high-contrast frame follows the hurt seat. Text size is still only the HUD slider, the partner ring and the devil marker still lean on colour against a zombie's, and "Num Add" is a name a player would not use. Real gains, each small; hold. |

**Top fixes for next loop**
1. (dim 8 +~0.5, dim 10 +~0.25, dim 6 nit) Make the record line true for more than ten runs. Keep the tally where the bests live: add `runs`, `bestLevel` and a short sample of the last eight `seconds` to `RoomRecord.byDifficulty[preset]` in `SaveData.recordRun`, and have `recordLine` read that instead of the global `history` (keep the history for the Progress tab only); guard the debrief's line on `runs > 1` as the card guards on `runs > 0`, and on a first run say nothing, since the row beneath already says it; put the "best N on Beginner" cross-preset line back on the card after the preset's opening line. Tests: three Expert runs then ten Beginner runs elsewhere still read `{ runs: 3, bestLevel: 21, medianSeconds: 20 }`; the merge in `importCode` sums runs and keeps the higher best. ~45 min.
2. (dim 7 +~0.5, dim 3 +~0.25, dim 11 nit) Announce the opening wave. Push the level banner (or a 3 s caption "LEVEL 20 · 105 ZOMBIES · 6 DEVILS" through the same `drawMessages` path) when a run starts above level 1, so the wave a preset start opens on is the one wave that is described; put the same count on the room card's preset line ("Expert opens at level 20 with x30 · 105 zombies, 6 devils" from `levelDef`), so the player sees the size before choosing. Then wire `startLevel` into the lobby's match block so a host can open an online practice run, the last piece of Loop 7's fix 2. ~45 min.
3. (dim 5 +~0.25, dim 4 +~0.25, dim 6 +~0.25) Quiet the unasked tick and square the new layouts. In `Menus`, tick on `focusin` only when a key or the pad moved the focus (set a flag in the arrow/d-pad handler and in `pointerover`, clear it after the tick) so a screen opening does not tick as it focuses its first control, and skip the knock for `select` and `input[type=checkbox]` on `click` (fire it on `change` instead). Give the three Controls hints a fixed two-line height so the columns' rows align; make How to Play fit at 1280×720 by trimming the left column's `dl` gaps (R, M and F3 on one "R · M · F3" row) or drop the claim; render NumpadAdd/Subtract/Multiply/Divide as "Num +", "Num −", "Num ×", "Num ÷"; style PRACTICE OFF as a `.badge` button rather than a key cap. ~45 min.

## Loop 9 — 331741b — 2026-09-08 — Overall 8.18

Tests: 75 pass / 0 fail (1 new: three Expert runs in r1 then twelve Beginner runs in r2 still read `{ runs: 3, bestLevel: 22, medianSeconds: 20 }` with the history capped at ten, and a progress code merged into another save sums the runs to 4 and keeps the higher best of 25). `npm run build`: clean (246.01 kB bundle, up from 244.79). `git status` clean, so the dev server served 331741b for the whole pass. Playtest: 16 scenarios; Beginner room 1 alive at tick 1501 with 13 kills, x9, full life, and at tick 2501 on level 10 with 24 kills, x20; Expert room 6 dead at tick 3000 with 4 kills at 1:00 (x30 peak, as last loop); Nightmare room 9 alive at tick 2501 on level 36 with 354 kills, x258 (12 kills last loop; the bot's variance, nothing under `packages/shared` changed); the debrief scenario in room 1 on Nightmare dies at 2:00 on level 35 with 406,900. Draw 0.5–3.2 ms (the 3.2 is the first capture at tick 151), simMs 0–0.16; shared co-op 2.0 ms at 15 poses. Fairness room 1 (`--fairness 3`): Beginner 180 s (all three at the cap, =), Intermediate 150 s (146/150/180, was 150/154/180), Expert 12 s (10/12/12, was 10/10/14), Nightmare 10 s (10/10/10, =). Fairness room 9 (`--fairness 2 --room 8`): Beginner 180/180, Intermediate 180/180, Expert 180/180 with 377 kills at level 23 (was 401), Nightmare 18/10 with 56 kills (was 98/10 with 397; variance again). Co-op (`--coop`): server on 127.0.0.1:9892, Ann (swat, HOST) and Ben (bond) in the lobby with two open seats and the match block showing room, mode, difficulty, speed and devils but no start level; `joinedAs: { host: "lobby", guest: "lobby" }`, `bothInWave: true` at tick 214; `reconnect`: seat 1 → 1, state `joined`, tick 214 → 266; rtt 1 ms, server tick 264, host unacked 3 / replayed 1 / corrections 2 (last 2.8 px), guest unacked 2 / replayed 1 / corrections 2 (last 2.7 px). In coop-host.png Ben is by the crate and in coop-guest.png Ann is at the top right, both on screen, so the off-screen ring is unexercised for the third loop running. By hand in the browser pane at 1280×720: the Controls tab's three columns start their first rows on one line (MOVE UP, MOVE UP, FIRE all at the same y; the pad's two-line hint sits inside the `.colhead` min-height), P2's NEXT and PREVIOUS WEAPON read ". / NUM +" and ", / NUM −"; the room card on Expert with a Nightmare best reads "no Expert run yet · best 351,100 on Nightmare · Expert opens at level 20 with x30 · 105 zombies, 5 devils" over five lines, a line taller than its row-mates; opening room 1 on Expert shows "-+-+-+- LEVEL 20 -+-+-+-" over the red rule with "105 ZOMBIES · 5 DEVILS" beneath while the HUD says "110 LEFT", so the wave a preset opens on is now the one described. howto.png at 1280×720 still cuts at TWO PLAYERS and BARRICADES: a row shorter, not a screen. debrief.png (a first run on this difficulty) shows no record line above the stats, so the duplicate is gone; the second-run line is judged from `line.runs > 1`. The menu sounds are judged from the code (headless, no ear): `focusByKey` is set only by the arrow handler and the pad's d-pad walk and cleared on every `focusin`, so a screen focusing its first control on open no longer ticks; `select` and checkbox knock on `change`; the new `quiet` helper is never reached with a truthy result because the `click` handler returns for any SELECT or INPUT two lines earlier, and Tab-moved focus does not tick.

| # | Dimension | Score | Evidence |
|---|---|---|---|
| 1 | Core feel and controls | 7.5 → 7.5 | No hunk in movement, aim, fire, weapon switching or the camera; `Input.ts` changed only the numpad glyphs in `keyName`. The Loop 3 lead slider is still a switch, the facing tick still 9·s px. Hold. |
| 2 | Feedback and juice | 8.0 → 8.0 | Nothing in this commit answers an action. Still one vignette for the worst seat only, no low-health tick cue, and no scenario reaches low life, so the heartbeat has not been seen since it was split by seat. Hold. |
| 3 | Readability and clarity | 8.5 → 8.5 | The gap Loop 8 withheld on is closed: `startRun` pushes `levelBanner(session.startLevel)` for 200 ticks when a run opens above level 1, and `Hud.drawMessages` puts the wave's size under it (seen in the pane: LEVEL 20 with "105 ZOMBIES · 5 DEVILS", agreeing with "110 LEFT"). Not moved because the rest of the row stands: the devil marker still differs from a zombie's by colour alone, the wave line is still 4·s px under the rule, and a run resumed from the parked slot (`RUN_KEY`) mid-wave gets no banner, so the one case where the player has forgotten the wave's size is the one not told. Hold at a strong 8.5. |
| 4 | Visual polish and consistency | 8.0 → 8.5 | Every nit Loop 8's row named is closed and seen: the three Controls columns align (`.colhead { min-height: 2.6em }`, the pad's hint shortened to two lines), PRACTICE OFF is a `button.badge.chip` with a brass hover and focus glow rather than a key cap, the debrief's record line is gone on a first run (debrief.png shows only the stats rows), "NUM +" and "NUM −" where "Num Add" was, and the hint margins are a class rather than inline styles. Remaining: How to Play still scrolls at 1280×720 after two loops that said it would fit (howto.png cuts at TWO PLAYERS and BARRICADES), and the room card's preset line now runs to five lines with a cross-preset best, making one card taller than its row-mates on the grid. |
| 5 | Audio | 7.5 → 7.5 | The unasked tick is quiet: `focusByKey` gates the `focusin` tick to key- or pad-moved focus, and a `select` or checkbox knocks once on `change` rather than on the click that opened it. Right, small, and unheard by the harness. Withheld: Tab is not among the keys that set `focusByKey`, so a keyboard user tabbing through a menu hears nothing while the arrows tick; the practice slider gives no sound at all (the `click` handler returns for every INPUT and `change` fires the knock only for a checkbox); the hidden-tab throttle stands; no low-health cue in the run. Hold. |
| 6 | Onboarding and UX flow | 8.5 → 8.5 | The card says more before a choice: the preset's opening wave size ("Beginner opens at level 1 with x1 · 10 zombies", rooms.png) and, on another preset, the best the player has here, so a save with a Nightmare best no longer reads as unplayed on Expert. The in-run banner answers "what is this wave" on a preset start. Against: How to Play still scrolls at 720, `quickDeath` still reads this run's clears rather than the player's record here (a veteran's quick death is still told to practise), tips are P1-only, and the card's five-line preset sentence is a lot to read on a grid of eighteen. Hold. |
| 7 | Difficulty curve and pacing | 7.5 → 8.0 | The tables, preset starts and drain are the original's and the numbers say what they said within the bot's noise (room 1 Expert 10/12/12 s, Nightmare 10/10/10; room 9 Expert at the cap with 377 kills, Nightmare 18/10). What Loop 8's fix 2 asked for is done: the wave a preset start opens on is announced with its size, in-run (LEVEL 20 · 105 ZOMBIES · 5 DEVILS, seen) and on the card before the choice, so a player who dies at 10 s on Expert in room 1 was told what was coming both times. The framing of the curve is now complete from card to banner to debrief; the curve itself is unchanged, so no more than the half. |
| 8 | Progression and replayability | 8.0 → 8.5 | The defect that held this at 8.0 is fixed at the root: `PresetRecord` carries `runs` and the last eight `times` beside the bests, `recordRun` bumps them, `recordLine` reads them and not the ten-entry `history`, `importCode` sums runs and concatenates times when two saves merge, and the parser tolerates codes without them (`runs: count(b['runs']) ?? times.length`). Under test: three Expert runs then twelve elsewhere still read runs 3 / best 22 / usually 0:20. The card names the best on another preset again. Nits: a save from before this loop starts its tally at zero (`runs ?? 0`), so a long-time player's card says "best 251,100 · level 20" with no run count until they play again; "usually" is the median of at most eight; still no cross-room summary, and the progress code grows for every preset played. |
| 9 | Spirit of the original | 8.5 → 8.5 | Nothing in the tables, the drain or the arsenal moved. The opening banner and the wave count are small additions the original did not have; the banner uses the original's "-+-+-+- LEVEL N -+-+-+-" text and changes no rule. Hold. |
| 10 | Performance and robustness | 8.5 → 8.5 | 75/75 with the aging test Loop 8 asked for by name, a clean build and tree, draw 0.5–3.2 ms, reconnect measured. The record line's data now survives the history cap and a merge. Withheld: `quiet` in `Menus` is dead code (the `click` handler returns for SELECT and INPUT before consulting it), the `focusByKey` gate, PRACTICE OFF, `quickDeath` and `startLevelLabel` are untested, and the pushed opening banner is not re-pushed for a resumed parked run. Hold. |
| 11 | Multiplayer | 8.0 → 8.0 | Reconnect measured again (seat 1 → 1, `joined`, 214 → 266), both clients in the wave, rtt 1 ms, corrections ≤ 2.8 px. The lobby's match block still has no start level (declined this loop, the third time it was asked for), a networked co-op debrief still records under the local save's preset, the host's reconnect is not driven, and the partner ring is unexercised for the third loop. Hold. |
| 12 | Accessibility and options | 8.5 → 8.5 | Numpad keys read as glyphs a keyboard prints, and the Controls tab's columns align so a rebind's row is findable at a glance. Text size is still only the HUD slider, the devil marker still leans on colour, and Tab-moved focus is silent where arrow-moved focus ticks. Hold. |

**Top fixes for next loop**
1. (dim 11 +~0.5, dim 6 nit, dim 8 nit) Finish the online practice run that three loops have asked for. Add `startLevel` to the lobby's match block (the Game tab's slider, host-only, "off" by default) and carry it in the match message to the server's `World` construction and every client's session, so a host can open the wave a preset skips with friends; push the opening banner there too. Then have a networked debrief record under the match's preset and start level rather than the local save's, and mark it practice when the start level is on, so an online run cannot inflate a Beginner best. `--coop` should print the match's start level and preset beside `bothInWave`. ~1 h.
2. (dim 2 +~0.5, dim 3 +~0.25, dim 5 +~0.25) See and hear low health. Give `tools/playtest.mjs` a `low-health` scenario that drains the player to 40 through the existing debug hook and captures a frame, so the split heartbeat is finally seen; add a low-health pulse to `AudioEngine` (a muffled 60 Hz thump on each beat, quieter than the mix, off above 25 % life) and a small pulse on the seat's life bar; give the devil's off-screen marker a shape (horned or diamond) as well as its red, so it reads without colour. ~1 h.
3. (dim 4 +~0.25, dim 5 +~0.25, dim 10 +~0.25) Square the last of the menus and delete the dead branch. Make How to Play fit at 1280×720 for real: measure the left column, shorten the Gamepad entry to a one-line pointer ("Gamepad: see Options, Controls") and the Two Players entry to two sentences, then screenshot at 720 before claiming it; cap the room card's `.best` at three lines by shortening the cross-preset sentence ("best 351,100 on Nightmare · Expert: level 20, x30, 105 zombies + 5 devils"); set `focusByKey` on Tab and Shift+Tab as on the arrows, give the range a soft tick on `input`, and remove `quiet` or make the `click` handler use it; add tests for `startLevelLabel`, `quickDeath` and the PRACTICE OFF handler's `setStartLevel(0)`. ~45 min.

## Loop 10 — 4bd1f03 — 2026-09-08 — Overall 8.30

Tests: 78 pass / 0 fail (3 new, `packages/shared/test/match.test.ts`: a match config with `startLevel: 15` opens the world at 15 with `multiplierForStart(15)` banked; 1, 99, NaN and absent all read as the preset, an Expert config with 99 opens at 20; `matchWorld` on Expert carries a level banner with "20", on Beginner nothing, in deathmatch no level message). `npm run build`: clean (246.87 kB bundle, up from 246.01). `git status` clean; the title's build stamp reads "4BE00A2", the commit the long-lived dev server was started on, but Vite serves the working tree live and the tree is 4bd1f03, so the served code is this commit and only the stamp is stale. Playtest: 18 scenarios in ~14 s; Beginner room 1 alive at tick 1501 with 25 kills, x24, full life; Intermediate room 3 dead at tick 2500 (level 10, 9 kills, 0:50); Expert room 6 dead at tick 3000 with 0 kills at 1:00 (x30 peak); Nightmare room 9 dead at tick 2500 on level 36 with 203 kills and 2,834,300 (alive with 354 kills last loop; the bot's variance, nothing under `packages/shared/src/sim` changed but `announceOpening`); the debrief scenario in room 1 on Nightmare dies at 2:00 on level 35 with 419,300. Draw 0.5–2.7 ms (the 2.7 is the first capture at tick 151), simMs 0; shared co-op 2.0 ms at 17 poses. The two new scenarios hold the frame at the beat's peak: low-health.png (life 40 of 200, tick 320) shows a red vignette from every edge, deepest at the corners, the floor still bone in the middle third, the over-head bar a red sliver; low-health-shared.png (P2 at 40, P1 at 200) puts the deep red on the right half where P2 stands and leaves P1's left edge faint, so the beat has a side. Fairness room 1 (`--fairness 3`): Beginner 180 s (all three at the cap, =), Intermediate 148 s (134/148/150, was 146/150/180), Expert 10 s (10/10/14, was 10/12/12), Nightmare 10 s (10/10/10, =). Fairness room 9 (`--fairness 2 --room 8`): Beginner 180/180, Intermediate 180/66 (was 180/180), Expert 180/176 with 340 kills at level 23 (was 180/180 with 377), Nightmare 180/100 with 644 kills at level 38 (was 18/10 with 56; variance, the bot's Nightmare runs in room 9 have read 98, 18 and 180 in three loops). Co-op (`--coop`): server on 127.0.0.1:9779, Ann (swat, HOST) and Ben (bond) in the lobby with two open seats; the match block now ends in a PRACTICE START row, and at 1280×720 that row is the one thing cut by the fold (coop-lobby.png shows the slider's thumb at the bottom edge), so the lobby scrolls where it fit last loop; `joinedAs: { host: "lobby", guest: "lobby" }`, the new `match: { difficulty: "beginner", mode: "coop", startLevel: 0 }`, `bothInWave: true` at tick 214; `reconnect`: seat 1 → 1, state `joined`, tick 214 → 264; rtt 1–2 ms, server tick 264, host unacked 4 / replayed 1 / corrections 2 (last 2.8 px), guest unacked 1 / replayed 1 / corrections 2 (last 2.7 px). The harness still plays Beginner from level 1, so the online practice start is proven by the unit tests and the diff, not by a driven match, and in coop-host.png Ben is by the crate and in coop-guest.png Ann is at the top right, both on screen, so the off-screen ring is unexercised for the fourth loop running. howto.png at 1280×720: the left column now ends at TWO PLAYERS with room to spare, but the right column is cut at BARRICADES mid-sentence ("Wall yourself in and the wave never"), so How to Play still scrolls at 720 after a third commit that said it fits. rooms.png: the room-1 card reads "NOT PLAYED · BEGINNER OPENS AT LEVEL 1 WITH X1 · 10 ZOMBIES" on three lines. Read from the code: `NetSession.beginMatch` builds with `worldFromConfig` and `cosmetics = false`, the server builds with `matchWorld` and forwards the banner as a `message` event (seq 1 > `lastForwardedSeq` 0), so the opening banner reaches both ends once; `Room.configure` runs a host's `startLevel` through `practiceStart`, so 1, 99 and NaN become 0; the thump in `main.ts` reads `world.players[seat]` for seat 0 rather than `localPlayerIndex` as `Hud` does, so in a networked match a guest hears the host's heartbeat and not their own; `Hud.heartbeat` is one half-sine per period (the second half of the period multiplies a clamped negative sine, so it is 0), a single thump every 1.2–0.7 s rather than the "two quick beats then a rest" its comment promises; the `input` listener ticks a range on every step, so a drag across the 60-level slider is 60 ticks; a resumed parked run still gets no banner (`restore` runs instead of `announceOpening`, and a snapshot carries no messages); `NetSession.recordsScores` is false, so a networked run records nothing at all, which makes Loop 9's "an online run could inflate a Beginner best" wrong and its debrief-preset half of fix 1 moot. The thump and the ticks are judged from the code (headless, no ear).

| # | Dimension | Score | Evidence |
|---|---|---|---|
| 1 | Core feel and controls | 7.5 → 7.5 | No hunk in movement, aim, fire, weapon switching or the camera; `Input.ts` untouched. The Loop 3 lead slider is still a switch, the facing tick still 9·s px. Hold. |
| 2 | Feedback and juice | 8.0 → 8.5 | The low-health signal is finally seen and answered in three places: the vignette is stronger (`0.3 + urgency·0.3` from `0.18 + 0.22`, a middle stop at 0.6 and the rim at up to 0.85) and sits on the hurt seat's side (low-health.png, low-health-shared.png); the life bar's outline beats with the same `heartbeat()` (`1 + beat·1.5` px, alpha 0.25–0.9); and a 70 Hz thump (`UI.Heart`, `thump(0, 0.16, 0.14, 70, 40)`) lands once per rise (`beat > 0.5 && lastBeat <= 0.5`). Withheld from more: the heart is a single thump, not the lub-dub the comment claims (the second half of the period is 0); a networked guest hears the host's beat (seat 0, not `localPlayerIndex`); `lastBeat` is never reset between runs; two low seats still merge into the worse one's vignette. |
| 3 | Readability and clarity | 8.5 → 8.5 | The stronger vignette and the pulsing bar make "how hurt am I" harder to miss, and a networked match now opens on a described wave (the banner is forwarded once). Not moved: the devil marker still differs from a zombie's by colour alone, the wave line is still 4·s px under the rule, a resumed parked run still gets no banner, and the lobby's practice level reads only after the server echoes it (`#lstartVal` is rendered, not updated on `input`). Hold. |
| 4 | Visual polish and consistency | 8.5 → 8.5 | The card's cross-preset line is three lines (rooms.png) and the lobby's new row matches its neighbours' `.row` grammar. Against: How to Play still scrolls at 1280×720 after a third commit that said it fits (howto.png cuts at BARRICADES mid-sentence; the left column was shortened, the right column was not measured), and the lobby, which fit last loop, now scrolls at 720 because the practice row sits under DEVILS (coop-lobby.png). One fixed, one regressed: hold. |
| 5 | Audio | 7.5 → 8.0 | The run's one silent signal has a sound: a muffled 70 Hz thump under the mix on each heartbeat while a seat is under 30 %, centred and unpositioned as a body sound should be. In the menus, Tab and Shift+Tab set `focusByKey` so keyboard focus ticks like the arrows, and a range ticks on `input`, so the practice slider is no longer mute. Judged from the code. Nits: the slider ticks on every step (a drag across 60 levels is 60 ticks of 25 ms; one per detent or a 40 ms floor would do); the guest's heartbeat is the host's; the hidden-tab throttle stands. |
| 6 | Onboarding and UX flow | 8.5 → 8.5 | The lobby's practice start reads "level N · nothing is recorded" or "off", disabled for guests, and the Gamepad and Two Players entries in How to Play are two lines each. But How to Play still cuts at 720, the lobby's new row is the one under the fold at 720, `quickDeath` still reads this run's clears rather than the player's record here, and tips are P1-only. Hold. |
| 7 | Difficulty curve and pacing | 8.0 → 8.0 | Tables, preset starts and drain untouched, and the numbers say so within the bot's noise: room 1 Beginner 180 ×3, Intermediate 134/148/150, Expert 10/10/14, Nightmare 10/10/10; room 9 Beginner 180/180, Intermediate 180/66, Expert 180/176 with 340 kills, Nightmare 180/100 with 644. The framing gained one piece (an online run opens on a described wave). Hold. |
| 8 | Progression and replayability | 8.5 → 8.5 | Nothing in `SaveData` changed; the card's line is shorter, not different. A practice run online counts for nothing, as the local one does, and the lobby says so. Hold. |
| 9 | Spirit of the original | 8.5 → 8.5 | Nothing in the tables, the drain or the arsenal moved. The heartbeat thump and the online practice start are additions the original did not have; both are off by default and change no rule. Hold. |
| 10 | Performance and robustness | 8.5 → 8.5 | 78/78 with the match tests, a clean build, draw 0.5–2.7 ms, reconnect measured, the server sanitising the host's start level and both ends agreeing on the world (the client restores the server's snapshot over the same `worldFromConfig`). The dead `quiet` is gone. Withheld: the guest-hears-host heartbeat bug shipped untested; "How to Play fits a screen" is the third such claim without a measurement; `startLevelLabel`, `quickDeath`, PRACTICE OFF and `heartbeat()` are untested; the `--coop` harness never sets a start level, so the feature it reports is exercised at 0 only. Hold. |
| 11 | Multiplayer | 8.0 → 8.5 | The request of four loops is done end to end: `MatchConfig.startLevel`, a host-only slider in the match block, `Room.configure` clamping through `practiceStart`, `matchWorld` on the server and the same `worldFromConfig` on every client, the opening banner forwarded once, and `--coop` printing the match it played (`match: { difficulty, mode, startLevel }`). Reconnect measured again (seat 1 → 1, `joined`, 214 → 264), rtt 1–2 ms, corrections ≤ 2.8 px. Withheld: the harness does not drive a practice start; a guest's heartbeat is the host's; the lobby's practice row is under the fold at 720 and its label waits for the server's echo; the host's reconnect is not driven; the partner ring is unexercised for the fourth loop. |
| 12 | Accessibility and options | 8.5 → 8.5 | Tab-moved focus ticks like arrow-moved focus, so a keyboard user hears the menus. Text size is still only the HUD slider, the devil marker still leans on colour, and the heartbeat has no visual-only or audio-only preference. Hold. |

**Top fixes for next loop**
1. (dim 2 +~0.25, dim 11 +~0.25, dim 10 +~0.25) Give the heartbeat to the seat that owns it and prove it. In `main.ts` pick seat 0 as `world.players[run.session.localPlayerIndex]` as `Hud` does, and reset `lastBeat` in `startRun` and on a match start; make `Hud.heartbeat` the lub-dub its comment promises (a second, 0.55-strength half-sine a quarter period after the first) and unit-test it (period 60 at ratio 0.3, 35 at 0, exactly two peaks per period, 0 above 0.3); then have `--coop` configure `startLevel: 15` before readying so `match.startLevel` reads 15, both clients' `world.level` is 15, and both carry the LEVEL 15 banner at `bothInWave`. ~45 min.
2. (dim 4 +~0.25, dim 6 +~0.25, dim 10 nit) Measure the fold before claiming it. In `tools/playtest.mjs` record `document.scrollingElement.scrollHeight - innerHeight` for the howto, options, multiplayer and coop-lobby captures in results.json and coop.json, and fail the scenario when it is positive at 1280×720; then make the numbers zero: cut the right column of How to Play (BARRICADES to one sentence, or drop the `dl` row gap to 0.6em), and put the lobby's PRACTICE START on the DEVILS row (checkbox, room name, slider, label) so the match block ends where it ended last loop. ~45 min.
3. (dim 5 +~0.25, dim 3 +~0.25, dim 12 +~0.25) Rate the slider's tick and finish the tests. Tick a range once per detent or with a 40 ms floor (`lastTick` in the `input` listener), and update `#lstartVal` on `input` locally so the host reads the level as they drag; give the devil's off-screen marker a horned or diamond shape as well as its red; add the tests three loops have asked for by name: `startLevelLabel` at 1, 10, 15, 20, 35 and 60, `quickDeath` true for a 20 s death with 0 clears and false with 3, and the PRACTICE OFF handler calling `setStartLevel(0)`. ~45 min.

## Loop 11 — c50e4d4 — 2026-09-08 — Overall 8.29

Tests: 81 pass / 0 fail (3 new: `Hud.heartbeatAt` has two peaks a period at 30 % and at 0, the dub weaker than the lub, and nothing above 30 %; `SaveData.startLevelLabel` at 0, 10, 15, 20, 35 and 60; `SaveData.isQuickDeath` true for a 20 s death with no clears at level 20, false with three clears, at 90 s, on a practice run and at level 1). `npm run build`: clean (247.48 kB bundle, up from 246.87). `git status` clean, so the dev server served c50e4d4 for the whole pass (the title's build stamp still reads 4BE00A2, the commit the long-lived server was started on). Playtest: 18 scenarios; Beginner room 1 alive at tick 1501 with 25 kills, x24, full life; Intermediate room 3 dead at tick 2500 (level 10, 12 kills, 0:50); Expert room 6 dead at tick 3000 with 1 kill at 1:00 (x30 peak); Nightmare room 9 alive at tick 2502 on level 35 with 181 kills, x88 and 2,022,400 (dead with 203 kills last loop; the bot's variance, nothing under `packages/shared` changed); the debrief scenario in room 1 on Nightmare dies at 2:00 on level 35 with 353,500. Draw 0–3.4 ms (the 3.4 is game-early at tick 1501), simMs 0–0.29; shared co-op 1.9 ms at 17 poses. The new `overflow` field, pixels a menu runs past a 1280×720 window: title 13, rooms 657 (the 18-card grid, expected to scroll), and 0 for character, options, howto, multiplayer, pause, quick-pause and the debrief; howto.png shows both columns ending above the panel's foot with ~140 px of room, so How to Play fits at 720 for the first time since Loop 6 asked for it, and the harness says so with a number rather than a claim. Fairness room 1 (`--fairness 3`): Beginner 180 s (all three at the cap, =), Intermediate 180 s (148/180/180, was 134/148/150), Expert 12 s (10/12/12, was 10/10/14), Nightmare 10 s (10/10/10, =). Fairness room 9 (`--fairness 2 --room 8`): Beginner 180/180, Intermediate 180/180 (was 180/66), Expert 180/180 with 396 kills at level 23 (was 180/176 with 340), Nightmare 98/10 with 396 kills at levels 37/35 (was 180/100 with 644; the bot's Nightmare runs in room 9 have now read 98, 18, 180 and 98). Co-op (`--coop`, first try): server on 127.0.0.1:9544, Ann (swat, HOST) and Ben (bond) with "2 OPEN SEATS" on one dashed line; the match block is three rows (ROOM · MODE, DIFFICULTY · GAME SPEED, DEVILS · PRACTICE START) and both buttons sit inside the panel with the fold below them (coop-lobby.png); `lobbyOverflow: 0` after the host set `startLevel: 15`; `match: { difficulty: "beginner", mode: "coop", startLevel: 15 }`; `levels: { host: 15, guest: 15, hostBanner: true, guestBanner: true }`; `bothInWave: true` at ticks 216/217 with both HUDs reading LEVEL 15 · 81 LEFT (coop-host.png, coop-guest.png, four devils on screen in each); `reconnect`: seat 1 → 1, state `joined`, tick 217 → 266; rtt 1–2 ms, server tick 265, host unacked 4 / replayed 1 / corrections 2 (last 2.8 px), guest unacked 0 / replayed 1 / corrections 3 (last 2.7 px). Ben and Ann are on screen in each other's captures, so the off-screen ring is unexercised for the fifth loop running. game-nightmare.png: two markers rest on the strip's top edge at x≈720–750 under the hint bar, the strip and the bar hiding the back half of each, so the devil's horns, which are behind the point, are the part that is covered; the marker is judged from `drawThreatMarkers` (a forward point at 1.15·size and two spikes at (−0.9, ±1.0)). Read from the code: the thump in `main.ts` now reads `world.players[run.session.localPlayerIndex]` for seat 0 and `bind()` zeroes `lastBeat`, and `bind()` runs for a parked resume and a match start as well as a fresh run; `heartbeatAt` is two half-sines an eighth of a period wide at phase 0 and 0.25, the dub at 0.55, and the thump's trigger is `beat > 0.5` on a rising edge, which the dub crosses for ~2 ticks at period 60 and ~1.2 at period 35, so the ear gets both beats but at one loudness (`audio.play('UI.Heart', …)` takes no strength); the range tick is floored at 60 ms; `#lstartVal` follows the drag locally. One regression: the commit replaced both `this.lastForwardedSeq = world.cosmeticSequence` lines in `Room.ts` with `= 0`. The first (match start, line 375) is the fix it describes and is why both banners arrived. The second is the last line of `collectEvents` (line 583, indented four spaces short of its block), which runs every tick, so once any popup or banner exists `cosmeticSequence > 0` is always true and every live popup (45 ticks) and message (130 ticks) is pushed again on every tick until it expires. The client drops the repeats (`lastPopupSeq`/`lastMessageSeq` in `NetSession.presentEvents`), so nothing doubles on screen, but `pendingEvents` is capped at `MAX_EVENTS` 64 per three-tick publish and sounds are pushed before popups, so a cluster kill that leaves fifteen score popups alive fills the buffer by the second tick and the third tick's sounds (the ones the other player caused) are lost, in exactly the moments that have the most of them; between bursts every client receives a steady few hundred duplicate events a second. Untested, unmeasured by `--coop` (which asserts the banner arrived, not how many times). The thump and the ticks are judged from the code (headless, no ear).

| # | Dimension | Score | Evidence |
|---|---|---|---|
| 1 | Core feel and controls | 7.5 → 7.5 | No hunk in movement, aim, fire, weapon switching or the camera; `Input.ts` untouched. The Loop 3 lead slider is still a switch, the facing tick still 9·s px. Hold. |
| 2 | Feedback and juice | 8.5 → 8.5 | The three things Loop 10 withheld for are done: the heartbeat is a lub and a softer dub (`heartbeatAt`, under test), the thump belongs to the seat this screen drives (`localPlayerIndex`), and `lastBeat` is zeroed in `bind()` so a new run cannot inherit a half-finished beat. The vignette and the bar's outline now pulse twice a period, which is the shape a player's own pulse has. Not the half point: the audio thump is the same loudness for both beats (no strength reaches `audio.play`), so the dub is only visually softer; the dub's trigger window is 1–2 ticks wide at the fastest period; two low seats still merge into the worse one's vignette; no capture shows the dub (low-health.png holds tick 313, one frame). Hold. |
| 3 | Readability and clarity | 8.5 → 8.5 | The devil's marker is a shape as well as a colour (a point and two rear spikes in `drawThreatMarkers`), the last colour-only marker named since Loop 6. Withheld from more because the one capture with a devil marker (game-nightmare.png) has it resting on the strip's top edge under the hint bar, where the strip covers the spikes and only the point shows: the exclusion keeps the marker off the strip but not off the hint bar, and the horns are the half that hides. A resumed parked run still gets no banner; the wave line is still 4·s px under the rule. Hold. |
| 4 | Visual polish and consistency | 8.5 → 9.0 | Both docks of Loop 10 are closed with a measurement, not a claim: `overflow` is 0 for How to Play (`.keys` gap 10 → 7, the two-column rules tighter, BARRICADES one sentence; howto.png ends with room under both columns) and 0 for the lobby (open seats as one "N open seats" line, ROOM · MODE, DIFFICULTY · GAME SPEED, DEVILS · PRACTICE START paired on three rows, the panel's own padding trimmed; coop-lobby.png has READY and START MATCH inside the frame). The harness prints the number for every screen and says so in red when it is not zero, so the fold cannot regress silently again. The paired rows read cleanly and keep the slab-and-brass grammar. Nits that keep it from more: the title reports 13 px (the hero `.inner`'s 70 px bottom padding past HOW TO PLAY; nothing visible is cut, but the harness flags it every run); the lobby's paired labels are inline `style="margin-left:18px"` rather than a class; the practice track is 160 px for 60 detents. |
| 5 | Audio | 8.0 → 8.0 | The heartbeat has its rhythm (two thumps a period, faster as life falls) and it is the listener's own seat online; the practice slider ticks at most every 60 ms (a drag across 60 detents is ~10 ticks, not 60). Against: the dub thumps as loud as the lub, so the "softer" half of the rhythm is not in the ear; and the `Room.ts` re-forwarding can drop the other player's sounds in a burst (dim 10). Judged from the code. Hold. |
| 6 | Onboarding and UX flow | 8.5 → 8.5 | How to Play is one screen at 1280×720, the lobby fits with its practice row, the host reads the level while dragging (`#lstartVal` on `input`) rather than after the server's echo, and the lobby names the open seats in one line. Still: `isQuickDeath` (now pure and tested) reads this run's clears rather than whether the player ever cleared the preset here, so a veteran's quick death is still told to practise; tips are P1-only; the coop-lobby capture is taken before the host sets the level, so the PNG shows "off" beside a slider at 1. Two folds closed, two named holes open: hold. |
| 7 | Difficulty curve and pacing | 8.0 → 8.0 | Tables, preset starts and drain untouched (no hunk under `packages/shared/src/sim`), and the numbers are within the bot's noise: room 1 Beginner 180 ×3, Intermediate 148/180/180, Expert 10/12/12, Nightmare 10 ×3; room 9 Beginner 180/180, Intermediate 180/180, Expert 180/180, Nightmare 98/10. An online practice run is now a driven, described wave (level 15 with its banner on both ends). Hold. |
| 8 | Progression and replayability | 8.5 → 8.5 | `SaveData` gained two pure statics and no change in what is recorded; a networked run still records nothing, as documented. Hold. |
| 9 | Spirit of the original | 8.5 → 8.5 | Nothing in the tables, the drain or the arsenal moved. The lub-dub is closer to the original's low-health pulse than a single thump was; the horned marker is an addition that changes no rule. Hold. |
| 10 | Performance and robustness | 8.5 → 8.0 | 81/81 with the tests three loops asked for by name, a clean build and tree, draw 0–3.4 ms, reconnect measured, the fold measured for every screen, and `--coop` now proves the practice start online (level 15 on both clients, both banners). Docked half a point for a regression that shipped in the server's per-tick path: `collectEvents` ends with `this.lastForwardedSeq = 0` (the replace hit both occurrences; the indentation shows it), so every live popup and banner is re-forwarded every tick, `pendingEvents` fills its 64-slot window in a burst and the remote seat's sounds are dropped in that window, and every client streams duplicate events between bursts. The client's seq guard hides it from the eye, no test covers `collectEvents`, and `--coop` counts banners as present rather than counting them. The title's 13 px is flagged as an error by the harness on every run. |
| 11 | Multiplayer | 8.5 → 8.5 | Real gains: the practice start is driven and proven end to end (`match.startLevel` 15, both `world.level` 15, both banners at `bothInWave`), the opening banner reaches both clients because the match-start reset is right, a guest hears their own heartbeat, the lobby fits at 720 with its label live under the drag, and the open seats read as one line. Reconnect again (seat 1 → 1, `joined`, 217 → 266), rtt 1–2 ms, corrections ≤ 2.8 px. Offset by the same regression: in a busy wave the remote player's sounds can be lost from the publish window, and the wire carries the banner ~130 times and each popup ~45 times. The host's reconnect is not driven; the partner ring is unexercised for the fifth loop. Hold. |
| 12 | Accessibility and options | 8.5 → 8.5 | The devil marker no longer leans on colour alone (a shape in both contrast modes), the slider is no longer a rattle under a keyboard hold, and the lobby's practice level is readable while it changes. Text size is still only the HUD slider, and the heartbeat has no visual-only or audio-only preference. Small, real; hold. |

**Top fixes for next loop**
1. (dim 10 +~0.5, dim 11 +~0.25, dim 5 nit) Forward each cosmetic once again and count it. In `Room.collectEvents` restore `this.lastForwardedSeq = world.cosmeticSequence` as its last line (the `= 0` belongs only at match start, line 375, where it is right) and fix the indentation; add a server test (`packages/server/test`) that a room whose world holds one live popup and one banner across three ticks forwards each exactly once and still forwards a tick-3 sound after a burst of twenty popups; and have `--coop` count the `popup` and `message` events each client received during the drive (`events: { host: { popups, messages }, guest: … }`) so a re-forward shows as a number beside `levels`. ~30 min.
2. (dim 5 +~0.25, dim 2 +~0.25, dim 3 nit) Put the dub in the ear and keep the horns in view. Pass the beat's strength to the thump (`audio.play('UI.Heart', …, { gain: beat })` or a `UI.HeartSoft` variant at 0.55) and trigger on a rising edge over 0.3 rather than 0.5 so the dub is not a one-tick window at period 35; draw both low seats' vignettes when two are under 30 % (each on its side, the period from the worse); and include the hint bar's rect in the marker exclusion (`stripExtents` already carries the strips) so a marker on the bottom edge sits above the bar with its spikes visible; then capture a frame with a devil off screen in the `low-health` or `game-nightmare` scenario and assert a marker with `devil: true` exists. ~45 min.
3. (dim 6 +~0.25, dim 4 nit, dim 10 nit) Read the record, not the run, and clear the last two overflow numbers. Make `isQuickDeath` take the player's record here (`recordLine(room, preset)`: quick-death leads only when `runs === 0` or `bestLevel < startLevel + UNLOCK_CLEARS`), with a test that a 20 s death after a best of level 25 on that preset does not lead with practice; trim `.menu.hero .inner`'s bottom padding so the title reads 0 and make the harness fail the run for any screen but `rooms` above 0; move the lobby's inline `margin-left` styles into a `.row .pair` class; take the coop-lobby capture after the host sets the level so the PNG shows the slider at 15 with its label. ~40 min.

## Loop 12 — 573ff9d — 2026-09-08 — Overall 8.38

Tests: 82 pass / 0 fail (1 new, `packages/server/test/forward.test.ts`: a room publishing every tick forwards its opening banner exactly once across three ticks, and after a burst of twenty popups on one tick a pistol sound pushed on the next arrives once; the frames are read through `decodeServerMessage` as a client would). `npm run build`: clean (247.85 kB bundle, up from 247.48). `git status` clean, so the dev server served 573ff9d for the whole pass (the title's build stamp still reads 4BE00A2, the commit the long-lived server was started on). Playtest: 18 scenarios, exit 0; Beginner room 1 alive at tick 1501 with 25 kills, x24, full life; Intermediate room 3 alive at tick 2501 on level 10 with 42 kills and x34 (dead at 2500 the last two loops; the bot's variance); Expert room 6 dead at tick 3000 with 1 kill at 1:00 (x30 peak); Nightmare room 9 dead at tick 2500 on level 35 with 10 kills and 233,300 (alive with 181 kills last loop; the bot's Nightmare runs in room 9 now read alive, dead, alive, dead across four loops, nothing under `packages/shared/src/sim` changed); the debrief scenario in room 1 on Nightmare dies at 2:00 on level 35 with 462,200, 94 % accuracy, a streak of 31. Draw 0–1.9 ms, simMs 0–0.04; shared co-op 1.3 ms at 17 poses. `overflow`: title 0 (was 13; `.menu.hero .inner`'s bottom padding 70 → 40 px), rooms 657 (the grid, exempt), 0 for every other screen; the harness now sets a failing exit code for any screen but `rooms` above 0, and exited 0. title.png: the rules above PLAY and below HOW TO PLAY are the buttons' 360 px, and each rule's diamond sits on the buttons' centre line where last loop the rules ran wider and the diamonds sat right of it (the user-reported bug). Fairness room 1 (`--fairness 3`): Beginner 180 s (150/180/180, was 180 ×3), Intermediate 180 s (158/180/180, was 148/180/180), Expert 12 s (10/12/12, =), Nightmare 10 s (10/10/12, was 10 ×3). Fairness room 9 (`--fairness 2 --room 8`): Beginner 180/180, Intermediate 180/180, Expert 180/180 with 376 kills at level 23 (was 396), Nightmare 100/10 with 455 kills at levels 37/35 (was 98/10; the bot's Nightmare runs in room 9 have read 98, 18, 180, 98 and 100). Co-op (`--coop`, first try): server on 127.0.0.1:9783, Ann (swat, HOST) and Ben (bond) with "2 OPEN SEATS"; coop-lobby.png is now taken after the host sets the level, so it shows the PRACTICE START slider at 15 with "level 15 · nothing is recorded" beside it, READY and START MATCH inside the frame, `lobbyOverflow: 0`; `match: { difficulty: "beginner", mode: "coop", startLevel: 15 }`; `levels: { host: 15, guest: 15, hostBanner: true, guestBanner: true }`; the new `events: { host: { popups: 6, messages: 3 }, guest: { popups: 3, messages: 2 } }`, single digits where last loop's per-tick re-forward would have put the banner at ~130 and each popup at ~45; `bothInWave: true` at tick 216 with both HUDs reading LEVEL 15 · 78 LEFT, x25, 13,500 (coop-host.png, coop-guest.png, three devils on the left in each); `reconnect`: seat 1 → 1, state `joined`, tick 217 → 267; rtt 1 ms, server tick 266, host unacked 2 / replayed 0 / corrections 2 (last 1.2 px), guest unacked 1 / replayed 1 / corrections 2 (last 1.1 px). Ben is on the host's screen at the top wall and Ann on the guest's at the top right, so the off-screen ring is unexercised for the sixth loop running. Seen in the captures and not before: coop-host.png carries "SHOTGUN: DOUBLE AMMO" and "UZI: LONG SHOT" in its strip and coop-guest.png carries nothing at the same tick; the captures are taken after the guest's socket is closed and re-joined (`reconnect` runs before the screenshots in `runCoop`), the rejoin restores a snapshot, and a snapshot carries no messages, so a reconnected player comes back to a blank strip, and the guest's 2 messages to the host's 3 says the second award was sent while its socket was down and never re-sent. This is the parked-resume-gets-no-banner hole of Loops 8–11, now seen online. low-health.png and low-health-shared.png: the markers on the bottom edge sit at y≈585, above the hint bar (which starts at ≈600) with the point and both rear spikes visible; last loop's capture had them under the bar with the spikes hidden (`drawThreatMarkers` now clamps y to `height − 112·s` within 240·s of centre). Read from the code: `Room.collectEvents` ends with `this.lastForwardedSeq = world.cosmeticSequence` again and the `= 0` stays only at match start; `NetSession.eventsSeen` counts `messages` before the seq guard (duplicates included, as its comment says) but `popups` after it (`this.eventsSeen.popups += 1` follows `this.lastPopupSeq = event.seq`), so a popup re-forward would not show in the number the harness prints for it; the thump fires on a rising edge over 0.3 and picks `UI.Heart` when the beat or the next tick's beat passes 0.6, else `UI.HeartSoft` (`thump(0, 0.12, 0.08, 80, 45)` against the lub's `0.16, 0.14, 70, 40`), and since the dub's peak is 0.55 the ear now gets a lub and a softer, shorter, slightly higher dub; `Hud.draw` calls `drawLowHealth` for every local seat under 30 % rather than the worst one, each centred on its own x; `isQuickDeath(result, record, startLevel)` returns false when `record.bestLevel >= startLevel + UNLOCK_CLEARS`, and `Menus` passes `recordLine(room, preset)` with `result.level − result.levelsCleared`, but `save.test.ts` still calls the two-argument form only, so the veteran case the fix asked for by name is untested; the lobby's paired labels are `label.pair`. The thump is judged from the code (headless, no ear).

| # | Dimension | Score | Evidence |
|---|---|---|---|
| 1 | Core feel and controls | 7.5 → 7.5 | No hunk in movement, aim, fire, weapon switching or the camera; `Input.ts` untouched. The Loop 3 lead is still a checkbox (`#lead`, "Camera leads the aim"), the facing tick still 9·s px (`Hud.ts:186`). Twelve loops at 7.5 on the heaviest weight. Hold. |
| 2 | Feedback and juice | 8.5 → 8.5 | Three of the four things Loop 11 withheld for are done: the dub is softer in the ear as well as on screen (`UI.HeartSoft`), the trigger is a rising edge over 0.3 so the dub is caught for ~3 ticks at the fastest period rather than 1–2, and two low seats on a shared screen each get their own vignette on their own side (`drawLowHealth` per seat). Not the half point: still no capture holds the dub (low-health.png is one frame at tick 313), the beat is heard by nobody in this harness, and the change reaches a player only in the last seconds of a life. Hold at a strong 8.5. |
| 3 | Readability and clarity | 8.5 → 8.5 | The devil marker keeps its horns in view: on the bottom edge it now sits above the hint bar with both spikes showing (low-health.png, low-health-shared.png), where last loop's capture hid them. Against: the reconnect capture shows the other half of the banner hole plainly (coop-guest.png has an empty strip while coop-host.png carries two awards at the same tick), a resumed parked run still gets no banner, and the wave line is still 4·s px under the rule. One closed, one newly seen: hold. |
| 4 | Visual polish and consistency | 9.0 → 9.0 | Every nit of Loop 11's row is closed and measured: the title reads 0 overflow, the lobby's paired labels are a class, and the title's rules are the buttons' width so their diamonds sit on the buttons' axis (title.png; the user reported the misalignment). The coop-lobby capture now shows the slider at 15 with its label. Remaining: the practice track is 160 px for 60 detents (lobby) and ~255 px in Options; the build stamp on the title is the server's start commit, not the served one. Nits only, and 9.5 needs more than nits removed: hold. |
| 5 | Audio | 8.0 → 8.5 | Both items Loop 11's row held against are closed: the dub is a distinct, softer thump (`UI.HeartSoft`, 0.12 s at 0.08 gain, 80 → 45 Hz against the lub's 0.16 s at 0.14, 70 → 40), so the rhythm the eye gets is the one the ear gets; and the other player's sounds can no longer be pushed out of the publish window in a burst, under a server test that a sound after twenty popups arrives exactly once. Judged from the code and the test, not the ear. Nits: the hidden-tab throttle stands; no capture or harness number touches the mix. |
| 6 | Onboarding and UX flow | 8.5 → 8.5 | Two of Loop 11's three named holes closed: `isQuickDeath` reads the player's record here (a best of level 23 or better on that preset in that room, and the debrief leads with PLAY AGAIN rather than practice), and the lobby capture shows the practice level the host set. Still: tips are P1-only (`offerTips(world, me)` for the local seat), the veteran case is untested, and a reconnected or resumed player comes back to a strip that has forgotten the wave. Hold. |
| 7 | Difficulty curve and pacing | 8.0 → 8.0 | Tables, preset starts and drain untouched (no hunk under `packages/shared/src/sim`), and the numbers are within the bot's noise: room 1 Beginner 150/180/180, Intermediate 158/180/180, Expert 10/12/12, Nightmare 10/10/12; room 9 Beginner 180/180, Intermediate 180/180, Expert 180/180, Nightmare 100/10. Hold. |
| 8 | Progression and replayability | 8.5 → 8.5 | `SaveData` gained a record-aware `isQuickDeath` and nothing changed in what is recorded; a networked run still records nothing, as documented. The record now shapes the debrief's offer, which is the record earning its keep, but no new reason to play again. Hold. |
| 9 | Spirit of the original | 8.5 → 8.5 | Nothing in the tables, the drain or the arsenal moved. The softer dub is nearer the original's low-health pulse; the title rules are dressing. Hold. |
| 10 | Performance and robustness | 8.0 → 8.5 | The regression Loop 11 docked for is fixed at the root and proven: `collectEvents` ends with `lastForwardedSeq = world.cosmeticSequence`, `forward.test.ts` decodes the room's real frames and asserts one banner across three ticks and one sound after a burst of twenty popups, and `--coop` prints the count each client was sent (6/3 and 3/2 where the bug would have printed hundreds). 82/82, clean build and tree, draw ≤ 1.9 ms, reconnect measured, every menu 0 overflow with the harness failing the run otherwise. Withheld from more: `eventsSeen.popups` is counted after the seq guard, so the harness number cannot show a popup re-forward (messages are counted before it); the veteran `isQuickDeath` case shipped without its test; a reconnected client loses the banners sent while its socket was down (the snapshot carries no messages). Restored to where it stood before the regression. |
| 11 | Multiplayer | 8.5 → 8.5 | The wire is clean again (each cosmetic once, sounds kept in a burst, counted per client in coop.json), the lobby capture shows the practice level, reconnect measured again (seat 1 → 1, `joined`, 217 → 267, corrections ≤ 1.2 px). Against, and seen for the first time: the reconnected guest's strip is blank while the host's carries two awards, because the rejoin snapshot carries no messages and the award sent during the outage is not re-sent; the host's reconnect is not driven; the partner ring is unexercised for the sixth loop; a networked run still records nothing. Hold. |
| 12 | Accessibility and options | 8.5 → 8.5 | On a shared screen each hurt seat's frame and vignette is its own (the high-contrast border still covers the hurt seat's half), and the marker's shape is no longer hidden by the hint bar, so the colour-independent cue is in view. Text size is still only the HUD slider, and the heartbeat has no visual-only or audio-only preference. Hold. |

**Top fixes for next loop**
1. (dim 1 +~0.5, dim 12 nit) Make the camera lead a degree, not a switch, and measure input latency. Replace `#lead` in Options › Feel with a range (0–100 %, the old boolean read as 0 or 100) stored as `save.cameraLead: number` and scale the camera's aim offset by it; in `tools/playtest.mjs` add a `latency` line to results.json: dispatch `keydown KeyD` and count the ticks until `world.players[0].x` moves, then a `mousedown` and the ticks until the first `Weapon.*.Fire` sound, so the number the heaviest dimension is judged on exists. Dimension 1 has sat at 7.5 for twelve loops on the largest weight; a graded lead and a measured latency are the two things a reviewer of the feel would ask for first. ~1 h.
2. (dim 11 +~0.25, dim 3 +~0.25, dim 10 nit) Bring the strip back with a reconnect and count popups honestly. Have the server's rejoin path send the world's live `messages` (with their remaining life) after the snapshot, or carry them in the snapshot, so a reconnected client's strip matches the host's; while there, re-push the opening banner for a resumed parked run (`LocalSession` with a snapshot calls `restore` and never `announceOpening`); move `eventsSeen.popups += 1` above the seq guard to match `messages`; and extend `forward.test.ts` with a client that leaves after an award and rejoins to find it. Then assert in `--coop` that the guest's strip holds the same messages as the host's after `reconnect`. ~45 min.
3. (dim 6 +~0.25, dim 10 nit, dim 11 nit) Tips for the second seat, the veteran test, and the ring at last. Call `offerTips` for every local seat (P2's hint bar entries keyed to its bindings) so the shared-screen newcomer, who is usually the guest, is taught; add the `isQuickDeath` test the fix asked for (a 20 s death at level 20 with `{ bestLevel: 25 }` on that preset is not a quick death, and with `{ bestLevel: 21 }` is); and in `runCoop` walk the guest away for the first 1.5 s (KeyA alone, no fire) before the shared drive, then assert a partner marker exists in `__game.debugStats()` for the host, so the off-screen partner ring is seen after six loops. ~40 min.

## Loop 13 — 2c2a33d — 2026-09-08 — Overall 8.44

Tests: 84 pass / 0 fail (2 new in `packages/client/test/save.test.ts`: a 20 s death at level 20 with `{ bestLevel: 25 }` on that preset is not a quick death and with `{ bestLevel: 21 }` or no record it is, the veteran case Loop 12 named as untested; and `cameraLead` reads 1 on a fresh save, 0.4 after `setCameraLead(0.4)`, and clamps 7 to 1). `npm run build`: clean (248.58 kB bundle, up from 247.85). `git status` clean, so the dev server served 2c2a33d for the whole pass (the title's build stamp still reads 4BE00A2, the commit the long-lived server was started on). Playtest: 19 scenarios (one new, `latency`), exit 0; Beginner room 1 alive at tick 1501 with 26 kills, x24, full life; Intermediate room 3 alive at tick 2501 on level 10 with 39 kills and x33; Expert room 6 dead at tick 3000 with 0 kills at 1:00 (x30 peak, 67 % accuracy); Nightmare room 9 dead at tick 2500 on level 35 with 15 kills and 314,500 (x57 peak; the bot's room 9 Nightmare runs now read alive, dead, alive, dead, dead); the debrief scenario in room 1 on Nightmare dies at 2:00 on level 35 with 619,500, 95 % accuracy, a streak of 27, most kills with the rocket. Draw 0–2.0 ms, simMs 0; shared co-op 1.5 ms at 17 poses. `overflow`: title 0, rooms 657 (the grid, exempt), 0 for every other screen. **`latency`: `moveTicks: 1, fireTicks: 1`**, a key down to the first step the player moves and a click to the first `Weapon.*` sound are each one 50 Hz simulation tick (20 ms), the floor for a fixed-step loop; latency.png is a black canvas with the hint bar because the scenario stops the loop before the frame is drawn, so the number has no picture beside it. title.png: logo, "2PLAY · SINGLE PLAYER · SURVIVE THE ROOMS", BEST 0 · ROOMS UNLOCKED 1 / 18, both rules and the five buttons share one 400 px axis with everything centred in it, the key art's crowd to the right (the user's report; `.menu.hero .column { width: 400px }`, the rules and buttons `max-width: 400px; margin: auto`). Options › Feel, opened by hand at localhost:5180: "CAMERA LEADS THE AIM" is now a range at 100 % between HUD SIZE and HIGH CONTRAST; its value label sits ~20 px right of the two sliders' above it because the track starts where the longer label ends, so the three percentages do not read down one column. Fairness room 1 (`--fairness 3`): Beginner 180 s (180 ×3, was 150/180/180), Intermediate 156 s (92/156/180, was 158/180/180), Expert 10 s (10/10/16, was 10/12/12), Nightmare 10 s (10 ×3, was 10/10/12). Fairness room 9 (`--fairness 2 --room 8`): Beginner 180/180, Intermediate 160/180 (was 180/180), Expert 180/180 with 383 kills at level 23 (was 376), Nightmare 96/10 with 427 kills at levels 35/37 (was 100/10 with 455). Co-op (`--coop`): the first attempt was run by the critic concurrently with the room 9 fairness pass and came back with both clients on the `multiplayer` screen, `match: null`, `events` null and `reconnect.state: "run ended before the reconnect"`, exit 0 with a complete coop.json, so the new tolerance for a run that ends early held on its first outing, though what closed both sockets (two headless browsers plus a server under a third browser's load) is not recorded anywhere the harness prints. The second attempt, alone: server on 127.0.0.1:9667, Ann (swat, HOST) and Ben (bond) with "2 OPEN SEATS"; coop-lobby.png shows the PRACTICE START slider at 5 with "level 5 · nothing is recorded", `lobbyOverflow: 0`; `match: { difficulty: "beginner", mode: "coop", startLevel: 5 }`; `levels: { host: 5, guest: 5, hostBanner: true, guestBanner: true }`; `events: { host: { popups: 1, messages: 1 }, guest: { popups: 1, messages: 1 } }`; **`partnerMarkerSeen: true`** with `partnerMarkers: 1` in both clients' stats at the end of the drive, the first time in seven loops the off-screen partner ring has been shown to draw; `strips: { host: [], guest: [] }`, equal but empty, because the capture is at tick 272 and the level-5 opening banner lives 200 ticks, so the comparison proves nothing about what a rejoin carries; `bothInWave: true` at tick 272 with both HUDs reading LEVEL 5 · 30 LEFT, x4, 500, 1 kill (coop-host.png, coop-guest.png, a devil at right in each, the guest's screen labelling Ann and the host's labelling nobody); `reconnect`: seat 1 → 1, state `joined`, tick 272 → 322; rtt 2 ms, server tick 321, host unacked 3 / replayed 2 / corrections 2 (last 2.8 px), guest unacked 1 / replayed 1 / corrections 2 (last 2.7 px). Read from the code: `aimLead(me, amount)` scales the capped 64 px lead by `save.cameraLead`, whose getter maps a number to [0, 1] and an old `false` to 0, `true`/absent to 1, and the range's `input` event writes it and calls `onFeel`; `Room.welcome` adds `messages` (seq, text, kind, life) beside the snapshot and `NetSession` pushes those above `lastMessageSeq` after `beginMatch` (which resets both seqs to 0, so a rejoin takes them all; the room's own forward is keyed on `lastForwardedSeq`, so nothing is sent twice), but `forward.test.ts` and `net.test.ts` gained no case for it; `LocalSession` now calls `announceOpening` after `restore` as well as on a fresh world (a snapshot carries no messages, so no duplicate, and a resumed level-1 run says nothing, as the original); `eventsSeen.popups += 1` now precedes the seq guard, matching `messages`; `offerTips` runs for `world.players[1]` when `localSeats >= 2`, but the TIPS texts name P1's number keys ("barrels (4)", "fake walls (6)", "charge packs (9)") and the second seat has no number keys (`SECOND_SEAT` cycles with `,` `.` or numpad ± and fires with Enter), so a P2 tip tells them a key they do not hold; `Hud.partnerMarkers` is assigned before the early return in `drawThreatMarkers` and read into `debugStats`. The lead's feel at 50 % and the tips are judged from the code (no capture of either).

| # | Dimension | Score | Evidence |
|---|---|---|---|
| 1 | Core feel and controls | 7.5 → 8.0 | The two things Loop 12 asked for by name are in: the camera lead is a degree (Feel tab range 0–100 %, `aimLead(local, save.cameraLead)` scaling the 64 px cap, an old boolean save reading 0 or 100, under test), and input latency is a measured number: `moveTicks: 1, fireTicks: 1`, one 20 ms tick from key to step and click to shot, the best a 50 Hz fixed step can do. After twelve loops at 7.5 the heaviest dimension has the evidence a feel reviewer asks for first. Not 8.5: the number is sim ticks, not frame-to-photon; latency.png is blank so nobody can see the frame it measured; no capture shows the lead at any setting but 100 %; the facing tick is still 9·s px. |
| 2 | Feedback and juice | 8.5 → 8.5 | No hunk in flashes, shove, blood, shake, popups or the heartbeat. The partner ring is now seen to draw (`partnerMarkerSeen: true`), a readability cue more than juice. Hold. |
| 3 | Readability and clarity | 8.5 → 8.5 | Two of the three holes named last loop are closed in code: a rejoining client's welcome carries the room's live banners and a resumed parked run announces its wave (`announceOpening` after `restore`); and the off-screen partner ring, unexercised for six loops, drew on both clients (`partnerMarkers: 1`). Against a half point: the strip fix is proven only by reading `Room.welcome` and `NetSession`, since `strips` compares two empty arrays at tick 272 and no test joins a client late to find a banner; the wave line is still 4·s px under the rule. A strong 8.5; a capture of a banner on a reconnected strip would tip it. |
| 4 | Visual polish and consistency | 9.0 → 9.0 | The title's user-reported misalignment is gone at the root: one 400 px column, logo to bottom rule, everything centred on it (title.png), 0 overflow. New nit: the Feel tab's three percentages do not line up in a column because each track starts after its label. Standing nits: the practice track is 160 px for 60 detents in the lobby; the build stamp is the server's start commit. Hold. |
| 5 | Audio | 8.5 → 8.5 | No audio hunk; `fireTicks: 1` says the shot's sound is on the first step after the click, which is the mix's timing proven rather than changed. Hold. |
| 6 | Onboarding and UX flow | 8.5 → 8.5 | The veteran `isQuickDeath` case is tested, a resumed run is told its wave, and the second local seat is offered tips, which was the ask. But the tip texts are P1's ("barrels (4)", "fake walls (6)", "charge packs (9)") and seat 2 has no number keys, so the newcomer at the arrows is now taught keys they cannot press; a half-done feature holds the score. Tips still share one seen-list, so P2 only ever sees what P1 has not already triggered. Hold. |
| 7 | Difficulty curve and pacing | 8.0 → 8.0 | Tables, preset starts and drain untouched (no hunk under `packages/shared/src/sim`), and the numbers are within the bot's noise: room 1 Beginner 180 ×3, Intermediate 92/156/180, Expert 10/10/16, Nightmare 10 ×3; room 9 Beginner 180/180, Intermediate 160/180, Expert 180/180, Nightmare 96/10. The co-op scenario opening on level 5 rather than 15 is the harness picking a wave a pair can survive, not a change to the curve. Hold. |
| 8 | Progression and replayability | 8.5 → 8.5 | Nothing recorded changed; a networked run still records nothing, as documented. The veteran test pins the record's role in the debrief. Hold. |
| 9 | Spirit of the original | 8.5 → 8.5 | Tables, drain and arsenal untouched. The lead as a degree with 100 % the default keeps the Loop 3 feel; 0 % is the original's fixed camera for anyone who wants it. Hold. |
| 10 | Performance and robustness | 8.5 → 8.5 | 84/84, clean build and tree, draw ≤ 2.0 ms, every menu 0 overflow, popups counted before the guard so a re-forward would now show, the veteran test in, and the harness's tolerance for an early-ended run proven on its first outing (the concurrent co-op attempt reported cleanly rather than throwing). Withheld from more: that attempt shows two clients can both fall back to the multiplayer screen under load and nothing prints why (no close code or reason in coop.json); the welcome's `messages` shipped without the server test Loop 12 asked for; latency.png is a blank capture. Hold. |
| 11 | Multiplayer | 8.5 → 8.5 | A rejoining client is handed the room's live banners with its welcome, the practice level is proven online at 5, the partner ring is seen at last (`partnerMarkerSeen: true`), reconnect measured again (seat 1 → 1, `joined`, 272 → 322, corrections ≤ 2.8 px, rtt 2 ms). Against: `strips` is `[]` against `[]`, so the loop's headline multiplayer fix is unproven by its own number; both clients dropped to the connect screen in the loaded run without a recorded cause; the host's reconnect is not driven; a networked run still records nothing. Hold. |
| 12 | Accessibility and options | 8.5 → 8.5 | The lead is a graded preference rather than a switch, persisted and clamped, with an old save honoured; a finer option, not a new one. Text size is still the HUD slider only; the heartbeat has no visual-only or audio-only preference. Hold. |

**Top fixes for next loop**
1. (dim 1 +~0.5, dim 4 nit) Show the lead and the latency frame. In `tools/playtest.mjs` have `latency()` step and draw one frame before returning (or not call `g.loop.stop()` until after the capture) so latency.png shows the frame the number was measured on; add a `lead` scenario that sets `save.setCameraLead(0.5)` and `1`, aims at a fixed screen point, steps until the camera settles and reports `camera.x − player.x` for each (expect ~32 and ~64 px along the aim), so the slider is proven to grade rather than switch; and in Options › Feel lay the rows as a three-column grid (label / control / value) so the percentages read down one axis. While there, scale the facing tick with HUD size. ~45 min on the heaviest weight.
2. (dim 6 +~0.25, dim 3 nit) Tips that speak to the seat they are shown to. Give each TIP a `text(seat)` or a `{key}` substitution resolved through the seat's bindings (`SECOND_SEAT` has no digits: say "cycle with , and ." for P2 where P1 reads "(4)"), prefix the hint with P1/P2 when two seats share the screen, and keep a seen-list per seat so the newcomer at the arrows is taught even after P1 has seen it all; then capture one P2 tip in `low-health-shared` (or a new `shared-tips` scenario) and assert the hint text names a key P2 holds. ~30 min.
3. (dim 11 +~0.25, dim 10 nit) Prove the strip carry with a non-empty number, and say why a run ended. In `runCoop` pull the guest's plug while a banner is alive (reconnect within the opening banner's 200 ticks, or wait for the first award before the plug) and assert `strips.guest` deep-equals `strips.host` and is non-empty; extend `packages/server/test` with a client that leaves after an award and finds it in its welcome `messages` with life remaining; and when `NetSession` falls back to the multiplayer screen, record the socket's close code and reason in `stats()` so a coop.json that reads "run ended" also reads why. ~45 min.

## Loop 14 — 306782d — 2026-09-08 — Overall 8.55

Tests: 85 pass / 0 fail (1 new, `packages/server/test/welcome.test.ts`: a room on Expert steps and publishes ten ticks, a third client joins, and its welcome carries a snapshot and a `level` banner with `0 < life < 200`, the server test Loop 12 asked for). `npm run build`: clean (249.54 kB bundle, up from 248.58). `git status` clean, so the dev server served 306782d for the whole pass (the title's build stamp still reads 4BE00A2, the commit the long-lived server was started on). Playtest: 21 scenarios (two new, `lead` and `shared-tips`), exit 0; Beginner room 1 alive at tick 1501 with 25 kills, x23, full life; Intermediate room 3 dead at tick 2500 on level 10 with 9 kills, 43 % accuracy and a streak of 2 (was alive with 39 kills last loop; the bot's room 3 runs now read alive, dead); Expert room 6 dead at tick 3000 with 1 kill at 1:00 (x30 peak); Nightmare room 9 alive at tick 2501 on level 36 with 294 kills and 5,057,100 (x193; was dead at 2500 on level 35); the debrief scenario in room 1 on Nightmare dies at 2:00 on level 35 with 527,700, 94 % accuracy, a streak of 15, most kills with the rocket. Draw 0–2.9 ms, simMs 0 (0.24 for shared co-op at 11 poses). `overflow`: title 0, rooms 657 (the grid, exempt), 0 for every other screen. **`latency`: `moveTicks: 1, fireTicks: 1`** and latency.png is no longer black: the frame the numbers were measured on, the player mid-arena with the pistol's muzzle flash, "10 LEFT · 0 KILLS", the hint bar and the PSTL slot. **`lead`: `lead0: 0, lead50: 32, lead100: 64`**, room 5 (the widest), the pointer at the right edge and the player held mid-arena for 30 settled steps at each setting: the slider grades the 64 px cap linearly rather than switching it. lead.png, though, is taken after `leadAt(0)` (the final `setCameraLead(1)` is not followed by a step), so the picture shows the camera dead on the player and no lean at all; the number has a frame beside it but not the frame it describes. **`shared-tips`: `tip: "<b>P2</b> &nbsp; a <b>crate</b> — walk over it for ammo, or health when you are hurt"`**, shared-tips.png shows the hint bar reading "P2   A CRATE — WALK OVER IT FOR AMMO, OR HEALTH WHEN YOU ARE HURT" under a P2-labelled second player; the crate tip carries no key, so the `keyFor` path ("(cycle with , and .)" for seat 1) is read from the code, not captured. Options › Feel, opened by hand at localhost:5180: the rows sit on a `160px 260px auto` grid and the three percentages (shake, HUD size, lead) read down one column at last; at 160 px "CAMERA LEADS THE AIM" wraps to two lines, and "SHOW THEM AGAIN" now fills the whole value column as a wide slab where the checkboxes' hints sit as small grey text. Fairness room 1 (`--fairness 3`): Beginner 180 s (174/180/180, was 180 ×3), Intermediate 148 s (114/148/152, was 92/156/180), Expert 10 s (10/10/14, was 10/10/16), Nightmare 10 s (10/10/12, was 10 ×3). Fairness room 9 (`--fairness 2 --room 8`): Beginner 180/180, Intermediate 180/180 (was 160/180), Expert 180/176 with 394 kills at level 23 (was 383), Nightmare 96/10 with 435 kills at levels 37/35 (was 427). Co-op (`--coop`), run alone, clean on the first attempt: server on 127.0.0.1:9624, Ann (swat, HOST) and Ben (bond) with "2 OPEN SEATS"; coop-lobby.png shows the PRACTICE START slider at 5 with "level 5 · nothing is recorded", `lobbyOverflow: 0`; `match: { difficulty: "beginner", mode: "coop", startLevel: 5 }`; `levels: { host: 5, guest: 5, hostBanner: true, guestBanner: true }`; `events: { host: { popups: 1, messages: 1 }, guest: { popups: 1, messages: 1 } }`; **`earlyReconnect: { seatBefore: 1, seatAfter: 1, state: "joined" }`** with the guest's socket closed while the opening banner was alive, and **`stripsAfterRejoin: { host: ["-+-+-+- LEVEL 5 -+-+-+-"], guest: ["-+-+-+- LEVEL 5 -+-+-+-"] }`, `stripCarried: true`**, the non-empty, equal strips Loop 13 asked for; `closes: { host: "", guest: "" }`, so the new close reason is recorded but has not yet fired (no run ended this pass; `lastNetClose` is set only in `onClosed`, the fall-back to the multiplayer screen, and the harness's own plug-pull rejoins without passing through it); `partnerMarkerSeen: true` with `partnerMarkers: 1` on both clients; `bothInWave: true` at tick 334 with both HUDs reading LEVEL 5 · 30 LEFT, x4, 500, 1 kill (coop-host.png labels Ben over the guest, coop-guest.png labels Ann over the host, each with a pack of zombies at the top wall and a devil's fireballs in flight); `reconnect`: seat 1 → 1, `joined`, tick 334 → 385; rtt 2 ms, server tick 383/384, host unacked 3 / replayed 0 / corrections 2 (last 2.4 px), guest unacked 1 / replayed 2 / corrections 2 (last 2.2 px). Read from the code: `Tip.text` is a string or `(seat) => string`, `keyFor(slot, seat)` returns "(4)" for seat 0 and "(cycle with , and .)" for seat 1 through `input.seatBKeys('prev'|'next')[0]` and `keyName`, but it does not consult `padSeat`, so a P2 on the gamepad is told the comma and period keys while they cycle with the bumpers; `offerTips(world, me, seat)` keys the seen-list `${id}@2` for the second seat, ticks the shared cooldown only from seat 0 and prefixes `<b>P1</b>`/`<b>P2</b>` when `localSeats >= 2`; `debugTip(seat)` clears the cooldown and the hint and returns `hint.innerHTML`; `NetSession.lastClose` is set on any `closed` state and printed in `stats()` as `closed <reason>`, `main.ts` mirrors it in `lastNetClose` for `debugStats`; `lastLead` is the lean applied on the last step, rounded into `debugStats().lead`; `Hud`, `Renderer` and everything under `packages/shared/src/sim` are untouched, so the facing tick is still 9·s px and the wave line 4·s px.

| # | Dimension | Score | Evidence |
|---|---|---|---|
| 1 | Core feel and controls | 8.0 → 8.5 | Loop 13 withheld the half point on four counts and two are closed: the lead is proven to grade, not switch (`lead0: 0, lead50: 32, lead100: 64`, a straight line through the 64 px cap), and latency.png shows the very frame `moveTicks: 1, fireTicks: 1` was measured on, muzzle flash and all. Twenty milliseconds from key to step and click to shot, a camera the player can dial from the original's fixed frame to a full lean, rebindable keys and a pad: the heaviest dimension now has the numbers and the pictures. Withheld from 9: the number is still sim ticks, not frame-to-photon; lead.png shows the 0 % camera, not the lean; the facing tick is still 9·s px, unscaled by HUD size. |
| 2 | Feedback and juice | 8.5 → 8.5 | No hunk in `Hud`, `Renderer`, flashes, shove, blood, shake, popups or the heartbeat. Hold. |
| 3 | Readability and clarity | 8.5 → 9.0 | Loop 13 named the one thing that would tip it, "a capture of a banner on a reconnected strip", and it is here: the guest's plug pulled while the level-5 banner was alive, back in seat 1 `joined`, and `stripsAfterRejoin` reading `["-+-+-+- LEVEL 5 -+-+-+-"]` on both clients (`stripCarried: true`), with `welcome.test.ts` pinning that a late joiner's banner arrives with its remaining life. Off-screen threat and partner markers drawing on both clients, a health bar and a name over the partner, the multiplier bar, the next-award tag, the wave strip that survives a rejoin: it is hard to name a peer in the genre that tells the player more. Standing nit: the wave line is 4·s px under the rule. |
| 4 | Visual polish and consistency | 9.0 → 9.0 | The Feel tab's percentages read down one column (the `160px 260px auto` grid), the nit Loop 13 raised. Two new ones in its place: "CAMERA LEADS THE AIM" wraps to two lines at 160 px, and "SHOW THEM AGAIN" stretches across the value column as a slab beside rows whose third column is small grey hint text. Standing nits: the 160 px practice track for 60 detents in the lobby; the build stamp is the server's start commit. Hold. |
| 5 | Audio | 8.5 → 8.5 | No audio hunk. Hold. |
| 6 | Onboarding and UX flow | 8.5 → 8.5 | The half-done feature Loop 13 held the score on is finished: tips speak to the seat (`text(seat)` and `keyFor`, "(cycle with , and .)" for the arrows), carry a P1/P2 prefix on a shared screen, keep a seen-list per seat, and a P2 tip is captured in shared-tips.png. Against a half point: the capture is the crate tip, which has no key in it, so the seat-resolved key text is proven only by reading `keyFor`; a P2 on the gamepad is told comma and period though they cycle with the bumpers (`keyFor` never looks at `padSeat`); and nothing else on the first minute moved. A strong 8.5. Hold. |
| 7 | Difficulty curve and pacing | 8.0 → 8.0 | Tables, preset starts and drain untouched (no hunk under `packages/shared/src/sim`) and the medians are within the bot's noise: room 1 Beginner 180 (174/180/180), Intermediate 148 (114/148/152), Expert 10 (10/10/14), Nightmare 10 (10/10/12); room 9 Beginner 180/180, Intermediate 180/180, Expert 180/176, Nightmare 96/10. The bot dying in room 3 at 0:50 with 43 % accuracy where it lived last loop is the bot, not the curve. Hold. |
| 8 | Progression and replayability | 8.5 → 8.5 | Nothing recorded changed; a networked run still records nothing, as documented. Hold. |
| 9 | Spirit of the original | 8.5 → 8.5 | Tables, drain and arsenal untouched; the second seat's tips name its own keys, which is what the original's shared keyboard did implicitly. Hold. |
| 10 | Performance and robustness | 8.5 → 8.5 | 85/85, clean build and tree, draw ≤ 2.9 ms, every menu 0 overflow, the welcome test in, the co-op harness clean on its first attempt with the early plug-pull added. Against more: `closes` is `""` on both clients, so the close reason Loop 13 asked for exists but has never been seen to fire, and a run that ends under load would still be the first to test it; lead.png is captured at the wrong setting; the build stamp is stale. Hold. |
| 11 | Multiplayer | 8.5 → 8.5 | The headline fix is now proven by its own number (`stripCarried: true`, the LEVEL 5 banner on both strips after a mid-banner rejoin) and by a server test; reconnect measured twice in one run (the early one and 334 → 385, seat 1 → 1 both times, corrections ≤ 2.4 px, rtt 2 ms). Against a half point: every multiplayer number is loopback at 2 ms, and no scenario has ever put delay on the wire, so prediction quality under real latency is unmeasured; the host's reconnect is still not driven; a networked run records nothing. Hold. |
| 12 | Accessibility and options | 8.5 → 8.5 | Nothing new to set; the Feel tab is easier to read. Text size is still the HUD slider only; the heartbeat has no visual-only or audio-only preference. Hold. |

**Top fixes for next loop**
1. (dim 11 +~0.5, dim 1 nit) Measure prediction under real latency. Give `runCoop` a `--delay <ms>` that holds every socket message on the server for that long (a dev-only `setTimeout` around `send` and around each inbound handler in the socket wrapper), run the drive at 80 and 150 ms, and report per client `unacked`, `replayed`, `corrections` and the last correction in px beside the 2 ms figures; drive the host's reconnect in the same run. The one multiplayer number a reviewer asks for and the harness has never produced. ~1 h.
2. (dim 6 +~0.25, dim 4 nit) Finish the seat-resolved tip and tidy the Feel grid. In `shared-tips` unlock the barrel for seat 1 through the debug hook so the captured hint contains "(cycle with , and .)", and have `keyFor` consult `input.padSeat === 1` to say "cycle with the bumpers" when P2 is on the pad; then widen the Feel label column to ~200 px so "CAMERA LEADS THE AIM" sits on one line and give `#resetTips` `justify-self: start` so it is a chip, not a slab. ~30 min.
3. (dim 1 nit, dim 10 +~0.25) Picture the lean, fire the close reason, scale the tick. In `lead()` set the lead back to 1, settle the camera and step once before `debugBot(0)` so lead.png shows the 64 px lean the number reports; add a scenario where the server drops the guest for good (close its socket from the server side with a reason) and assert `closes.guest` is non-empty and the multiplayer screen shows it; and scale the facing tick with `hudScale` in `Hud`. ~40 min.

## Owner's correction — 2026-09-08 — after loop 14

Not a scored loop. The owner reviewed loops 9-14 and asked for three things,
which are done in the commit that carries this entry:

- **Local two-player on one screen is removed**, and is out of scope from here
  on (see the rubric). Gone: the room-screen toggle and second face, the second
  seat's keys and its column under Options › Controls, the pad-as-seat-two
  routing, the shared-screen camera, view scale and tether, the two weapon
  strips and facing tick, the per-seat heartbeat and tips, local deathmatch
  and its debrief, and the shared scenarios in the harness. Fix 2 of loop 14's
  list (seat-resolved tips) is void. Online co-op and deathmatch are untouched.
- **Block shadows** were a hard-edged band starting partway down a wall's side,
  with a darker square where the two gradients overlapped (a user screenshot
  under the pause overlay). They are now one blurred silhouette per arena,
  drawn once into the floor.
- **Options › Controls**: the reset button's drop shadow reached up over the
  last row of keys; the grid now clears it, and it lays out as two columns.

## Owner's direction — 2026-09-08 — controls and HUD overhaul

Not a scored loop. At the owner's request the controls and HUD now follow
modern shooters, and the grenade meets the arena by height:

- **Grenade**: on its own key (G, LT on a pad), held to charge; it flies over
  anything shorter than it is at that moment (cubes, fake walls, barrels) and
  bounces off anything taller (wall posts, the arena's edge). A dashed arc
  with the landing point and blast reach shows while the key is held. One
  integrator (packages/shared/src/sim/Grenade.ts) serves the sim and the arc.
- **Two slots**: primary (a gun) on 1, secondary (a placeable) on 2; tap to
  take it in hand, hold for a radial wheel and release toward a choice.
  Q/E and the mouse wheel cycle guns only. Right click uses the secondary
  without swapping. The grenade is never the weapon in hand.
- **HUD**: loadout cards with drawn weapon icons bottom right, squad bars with
  the characters' heads bottom left, a player-centred minimap top left.
- **Pings**: F pings the aim; held, a wheel of marks (enemy, going here,
  crate, hold here, help, devil). Relayed through the server as mark messages, rate-limited to four a second; drawn in the arena, at the edge,
  and on the minimap. The co-op harness reports pingRelayed.
- Harness scenarios grenade-arc, wheel and ping capture the new HUD.
